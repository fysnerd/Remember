---
phase: 08-theme-quiz-existing-cards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/routes/review.ts
  - backend/src/routes/themes.ts
  - ios/app/quiz/theme/[id].tsx
  - ios/hooks/useQuiz.ts
  - ios/hooks/index.ts
  - ios/types/content.ts
  - ios/app/theme/[id].tsx
  - ios/app/_layout.tsx
autonomous: true

must_haves:
  truths:
    - "User can tap Quiz on a theme detail screen and receive questions drawn from all content items within that theme"
    - "Questions in a theme quiz come from multiple different content items, shuffled across the theme's content"
    - "Theme quiz button is disabled with explanation when the theme has fewer than 3 content items with generated quizzes"
  artifacts:
    - path: "backend/src/routes/review.ts"
      provides: "POST /reviews/practice/theme endpoint"
      contains: "practice/theme"
    - path: "backend/src/routes/themes.ts"
      provides: "quizReadyCount and canQuiz fields in GET /:id response"
      contains: "quizReadyCount"
    - path: "ios/app/quiz/theme/[id].tsx"
      provides: "Theme quiz screen"
      min_lines: 80
    - path: "ios/hooks/useQuiz.ts"
      provides: "useThemeQuiz hook"
      exports: ["useThemeQuiz"]
    - path: "ios/types/content.ts"
      provides: "quizReadyCount and canQuiz on ThemeListItem"
      contains: "canQuiz"
  key_links:
    - from: "ios/app/theme/[id].tsx"
      to: "ios/app/quiz/theme/[id].tsx"
      via: "router.push with themeId"
      pattern: "quiz/theme"
    - from: "ios/app/quiz/theme/[id].tsx"
      to: "ios/hooks/useQuiz.ts"
      via: "useThemeQuiz(id)"
      pattern: "useThemeQuiz"
    - from: "ios/hooks/useQuiz.ts"
      to: "backend/src/routes/review.ts"
      via: "api.post /reviews/practice/theme"
      pattern: "practice/theme"
    - from: "backend/src/routes/review.ts"
      to: "prisma contentThemes join"
      via: "contentThemes: { some: { themeId } }"
      pattern: "contentThemes.*some.*themeId"
---

<objective>
Add theme-scoped quiz functionality: a backend endpoint that aggregates existing per-content quiz cards across all content items in a theme, and an iOS screen that presents the quiz using existing quiz components.

Purpose: Users can practice quiz questions scoped to a theme, reinforcing learning across related content.
Output: Working theme quiz flow (API + iOS screen) with minimum content threshold enforcement.
</objective>

<execution_context>
@C:/Users/vmsan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/vmsan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-theme-quiz-existing-cards/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend theme quiz endpoint and quiz readiness enrichment</name>
  <files>backend/src/routes/review.ts, backend/src/routes/themes.ts</files>
  <action>
  1. In `backend/src/routes/review.ts`, add a new `POST /practice/theme` endpoint AFTER the existing `POST /practice/topic` endpoint (line ~592). Follow the exact same pattern as `practice/topic` but scoped via `ContentTheme` join table instead of `Tag`:
     - Accept `{ themeId }` in request body (validate with Zod: `z.object({ themeId: z.string() })`)
     - Verify theme ownership: `prisma.theme.findFirst({ where: { id: themeId, userId } })`
     - Return 404 if theme not found
     - Find all READY contents in this theme with quizzes: query `prisma.content.findMany` with `where: { userId, status: 'READY', contentThemes: { some: { themeId } }, quizzes: { some: {} } }`, select only `{ id: true, title: true }`
     - Enforce minimum threshold: if `contents.length < 3`, return 400 with `{ error: 'Not enough content with quizzes', contentWithQuizzes: contents.length, minimum: 3 }`
     - Get cards: `prisma.card.findMany` where `quiz.contentId` is in the content IDs, include quiz with content select (id, title, url, platform) -- same include pattern as `practice/topic`
     - Shuffle cards with `cards.sort(() => Math.random() - 0.5)`
     - Cap at 20 questions: `shuffledCards.slice(0, 20)` -- prevents overwhelming sessions for themes with many contents
     - Return `{ cards: cappedCards, count: cappedCards.length, theme: { id: theme.id, name: theme.name, emoji: theme.emoji }, contentCount: contents.length, isPractice: true }`

  2. In `backend/src/routes/themes.ts`, enrich the `GET /:id` endpoint response with quiz readiness data:
     - Add a third parallel query inside the existing `Promise.all` (currently has `[contents, total]`, change to `[contents, total, quizReadyCount]`):
       ```
       prisma.content.count({
         where: {
           userId,
           contentThemes: { some: { themeId } },
           status: 'READY',
           quizzes: { some: {} },
         },
       })
       ```
     - Add `quizReadyCount` and `canQuiz: quizReadyCount >= 3` to the theme object in the response, alongside the existing `contentCount` and `tags` fields
  </action>
  <verify>
  Build the backend: `cd C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/backend && npx tsc --noEmit`
  No TypeScript errors.
  </verify>
  <done>
  - POST /reviews/practice/theme accepts themeId, returns shuffled cards from all content in theme (capped at 20), enforces 3-content minimum with 400 error
  - GET /themes/:id response includes quizReadyCount (number) and canQuiz (boolean) fields
  - Theme ownership verified on both endpoints (userId in where clause)
  </done>
</task>

<task type="auto">
  <name>Task 2: iOS theme quiz screen, hook, types, and smart quiz button</name>
  <files>ios/hooks/useQuiz.ts, ios/hooks/index.ts, ios/types/content.ts, ios/app/quiz/theme/[id].tsx, ios/app/theme/[id].tsx, ios/app/_layout.tsx</files>
  <action>
  1. In `ios/types/content.ts`, add two fields to the `ThemeListItem` interface:
     - `quizReadyCount: number;` -- content items with generated quizzes
     - `canQuiz: boolean;` -- quizReadyCount >= 3

  2. In `ios/hooks/useQuiz.ts`, add a `useThemeQuiz` hook after the existing `useTopicQuiz` function:
     - Define `ThemePracticeResponse` interface: `{ cards: BackendCard[]; count: number; theme: { id: string; name: string; emoji: string }; contentCount: number; }`
     - Export `useThemeQuiz(themeId: string)` that returns `useQuery` with:
       - queryKey: `['quiz', 'theme', themeId]`
       - queryFn: POST to `/reviews/practice/theme` with `{ themeId }`, then call `transformCardsToQuiz(data.cards, 'theme:' + themeId)`, return `{ ...quiz, theme: data.theme, contentCount: data.contentCount }`
       - enabled: `!!themeId`

  3. In `ios/hooks/index.ts`, add `useThemeQuiz` to the useQuiz export line:
     - Change `export { useQuiz, useTopicQuiz, useSubmitAnswer, useCreateSession, useCompleteSession } from './useQuiz';`
     - To: `export { useQuiz, useTopicQuiz, useThemeQuiz, useSubmitAnswer, useCreateSession, useCompleteSession } from './useQuiz';`

  4. Create `ios/app/quiz/theme/[id].tsx` -- the theme quiz screen. Copy the exact structure and state machine from `ios/app/quiz/topic/[name].tsx` with these differences:
     - Import `useThemeQuiz` instead of `useTopicQuiz` from hooks
     - Use `useLocalSearchParams<{ id: string }>()` to get `id` (not `name`)
     - Call `useThemeQuiz(id)` instead of `useTopicQuiz(decodedName)`
     - In the header, show `quiz.theme?.emoji` + `quiz.theme?.name` as the subtitle instead of `decodedName`
     - For `handleViewMemo`: route to `/theme/[id]` with `params: { id }` (theme detail, not memo -- theme memos come in Phase 9). Use `router.replace` like topic does.
     - For `handleClose`: route to `/(tabs)` same as topic quiz
     - Create session with `createSessionMutation.mutate({})` (no topicName, no contentId -- generic practice session for tracking)
     - Keep all other logic identical: state machine (question/feedback/summary), handleValidate (submitMutation with cardId, isCorrect, responseTime, sessionId), handleNext, handleQuit, QuestionCard, AnswerFeedback, QuizSummary

  5. In `ios/app/_layout.tsx`, register the new quiz/theme route. Add a new `Stack.Screen` entry AFTER the existing `quiz/topic/[name]` entry:
     ```
     <Stack.Screen
       name="quiz/theme/[id]"
       options={{
         headerShown: false,
         presentation: 'card',
       }}
     />
     ```

  6. In `ios/app/theme/[id].tsx` (theme detail screen), update the quiz button:
     - Read `canQuiz` and `quizReadyCount` from `data?.theme` (now returned by API)
     - Change `handleStartQuiz` to route to `quiz/theme/[id]` with `params: { id: id! }` instead of the current `quiz/topic/[name]` hack
     - Make the Button `disabled={!canQuiz}` and change button label:
       - When `canQuiz` is true: `Quiz {theme?.name}`
       - When `canQuiz` is false: `Quiz ({quizReadyCount ?? 0}/3 contenus)`
     - Add a hint Text below the button when `!canQuiz`: "Il faut au moins 3 contenus avec quiz pour lancer un quiz theme." using `variant="caption"` and `color="secondary"` with `marginTop: spacing.xs`
     - Show the quiz button section even when `!canQuiz` (move it outside the `contents.length === 0` conditional, or add it to the empty state too so it's always visible)
  </action>
  <verify>
  Verify TypeScript compiles: `cd C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/ios && npx tsc --noEmit` (or `npx expo export --platform ios --dump-sourcemap false` if tsc is not configured).
  Verify the new file exists: `ls ios/app/quiz/theme/[id].tsx`
  Verify the hook export: grep for "useThemeQuiz" in `ios/hooks/index.ts`
  </verify>
  <done>
  - ThemeListItem type includes quizReadyCount and canQuiz fields
  - useThemeQuiz hook fetches cards from POST /reviews/practice/theme and transforms to Quiz format
  - Quiz screen at quiz/theme/[id].tsx presents questions with same UX as topic quiz (question -> feedback -> summary)
  - Theme detail screen quiz button routes to quiz/theme/[id] (not quiz/topic/[name])
  - Quiz button is disabled with "X/3 contenus" label and hint text when canQuiz is false
  - Route registered in _layout.tsx
  </done>
</task>

</tasks>

<verification>
1. Backend builds without errors: `cd backend && npx tsc --noEmit`
2. iOS compiles without errors
3. POST /reviews/practice/theme returns shuffled cards from theme content (capped at 20)
4. POST /reviews/practice/theme returns 400 when theme has < 3 content with quizzes
5. POST /reviews/practice/theme returns 404 for non-existent or other user's theme
6. GET /themes/:id includes quizReadyCount and canQuiz in response
7. Theme detail quiz button correctly enables/disables based on canQuiz
8. Tapping enabled quiz button navigates to quiz/theme/[id] screen
9. Quiz screen presents questions, accepts answers, shows summary
10. SM-2 stats update on answer submission (no isPractice flag)
</verification>

<success_criteria>
- User taps Quiz on a theme detail screen with 3+ quizzable content items and sees shuffled questions from across the theme's content
- User sees disabled quiz button with "X/3 contenus" text on themes with insufficient quizzable content
- Theme quiz answers update SM-2 scheduling (consistent with topic/content quiz behavior)
- Maximum 20 questions per theme quiz session
</success_criteria>

<output>
After completion, create `.planning/phases/08-theme-quiz-existing-cards/08-01-SUMMARY.md`
</output>
