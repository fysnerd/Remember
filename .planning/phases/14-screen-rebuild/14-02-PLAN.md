---
phase: 14-screen-rebuild
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/components/explorer/SuggestionCard.tsx
  - ios/components/explorer/SearchInput.tsx
  - ios/hooks/useContent.ts
  - ios/hooks/useDebouncedValue.ts
  - ios/stores/contentStore.ts
  - ios/app/(tabs)/library.tsx
autonomous: true

must_haves:
  truths:
    - "Explorer screen has a Suggestions tab showing a placeholder state with Sparkles icon and 'coming soon' message"
    - "Explorer screen has a Library tab with the existing Collection/Triage sub-toggle preserved"
    - "Library tab supports full-text search via a GlassInput-based search bar that queries the backend"
    - "Library tab retains source/category filter pills and batch triage selection"
    - "Tab toggle between Suggestions and Library uses underline-style with accent color indicator"
    - "Inbox count badge visible on the Triage sub-tab within Library"
  artifacts:
    - path: "ios/components/explorer/SuggestionCard.tsx"
      provides: "GlassCard-based suggestion card (ready for Phase 15 data)"
      min_lines: 20
    - path: "ios/components/explorer/SearchInput.tsx"
      provides: "Search bar component wrapping GlassInput with Search icon and clear button"
      min_lines: 25
    - path: "ios/hooks/useDebouncedValue.ts"
      provides: "Generic debounce hook for search input"
      exports: ["useDebouncedValue"]
    - path: "ios/app/(tabs)/library.tsx"
      provides: "Rebuilt Explorer screen with Suggestions + Library tabs"
      min_lines: 120
  key_links:
    - from: "ios/app/(tabs)/library.tsx"
      to: "ios/components/explorer/SearchInput.tsx"
      via: "import SearchInput"
      pattern: "SearchInput"
    - from: "ios/app/(tabs)/library.tsx"
      to: "ios/hooks/useDebouncedValue.ts"
      via: "import useDebouncedValue"
      pattern: "useDebouncedValue"
    - from: "ios/hooks/useContent.ts"
      to: "backend GET /api/content?search="
      via: "search parameter in ContentFilters appended to URLSearchParams"
      pattern: "params\\.append.*search"
    - from: "ios/app/(tabs)/library.tsx"
      to: "ios/stores/contentStore.ts"
      via: "activeExplorerTab state for Suggestions/Library toggle"
      pattern: "activeExplorerTab"
---

<objective>
Rebuild the Explorer screen (library.tsx) with a two-level tab architecture: top-level Suggestions/Library tabs, and a Library sub-tab preserving the existing Collection/Triage toggle with search capability.

Purpose: Explorer is the content discovery hub. The Suggestions tab (placeholder until Phase 15) signals AI-powered discovery. The Library tab preserves all existing triage functionality while adding full-text search.

Output: Rebuilt library.tsx with SuggestionCard and SearchInput components, useDebouncedValue hook, wired backend search via useContentList
</objective>

<execution_context>
@C:/Users/vmsan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/vmsan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-screen-rebuild/14-RESEARCH.md
@ios/theme.ts
@ios/components/glass/GlassCard.tsx
@ios/components/glass/GlassInput.tsx
@ios/components/EmptyState.tsx
@ios/components/content/ContentCard.tsx
@ios/components/content/FilterBar.tsx
@ios/components/content/SourcePills.tsx
@ios/components/content/SelectionBar.tsx
@ios/hooks/useContent.ts
@ios/hooks/useInbox.ts
@ios/stores/contentStore.ts
@ios/app/(tabs)/library.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SearchInput, SuggestionCard, useDebouncedValue, and wire search into useContentList</name>
  <files>
    ios/components/explorer/SearchInput.tsx
    ios/components/explorer/SuggestionCard.tsx
    ios/hooks/useDebouncedValue.ts
    ios/hooks/useContent.ts
    ios/hooks/index.ts
    ios/stores/contentStore.ts
  </files>
  <action>
Create 3 new files and modify 3 existing:

**ios/hooks/useDebouncedValue.ts** -- Generic debounce hook:
```typescript
import { useState, useEffect } from 'react';
export function useDebouncedValue<T>(value: T, delay: number): T {
  const [debounced, setDebounced] = useState(value);
  useEffect(() => {
    const timer = setTimeout(() => setDebounced(value), delay);
    return () => clearTimeout(timer);
  }, [value, delay]);
  return debounced;
}
```
Add `export { useDebouncedValue } from './useDebouncedValue';` to `ios/hooks/index.ts`.

**ios/components/explorer/SearchInput.tsx** -- Search bar:
- Props: `value: string`, `onChangeText: (text: string) => void`, `placeholder?: string`.
- Renders a View with row layout: Search icon (from lucide-react-native, size 18, color textSecondary) on the left, TextInput in the middle (flex: 1), and a conditional X (CircleX icon) clear button on the right when value is not empty.
- Style: glass.border border, borderRadius.md, height 40 (smaller than standard input), overflow: hidden with BlurView behind (same pattern as GlassInput but custom layout for the icon).
- TextInput uses fonts.regular, fontSize 14, colors.text, placeholderTextColor: colors.textSecondary, keyboardAppearance: 'dark'.
- Default placeholder: "Rechercher...".
- Do NOT use GlassInput directly (it doesn't support left icon). Build a lightweight custom component using BlurView + TextInput.

**ios/components/explorer/SuggestionCard.tsx** -- Placeholder card for Phase 15:
- Props: `title: string`, `description?: string`, `onPress?: () => void`.
- Renders GlassCard with padding="md" and onPress.
- Shows title (Text variant="body" weight="medium") and optional description (Text variant="caption" color="secondary").
- This component is ready for Phase 15 data but will not be used in the Suggestions tab yet (the tab shows EmptyState instead).
- Add a `// TODO: Wire to GET /api/themes/suggestions in Phase 15` comment.

**Modify ios/hooks/useContent.ts** -- Add search to ContentFilters:
- Add `search?: string` to the `ContentFilters` interface.
- In `useContentList()`, add: `if (filters?.search) params.append('search', filters.search);` before the API call.
- This wires the existing backend `?search=` parameter that already supports full-text search on title, description, transcript.

**Modify ios/hooks/index.ts** -- Add new exports:
- Add `export { useDebouncedValue } from './useDebouncedValue';`

**Modify ios/stores/contentStore.ts** -- Add explorer tab state:
- Add `activeExplorerTab: 'suggestions' | 'library'` to the state interface (default: 'library').
- Add `setActiveExplorerTab: (tab: 'suggestions' | 'library') => void` to the interface.
- Add `searchQuery: string` and `setSearchQuery: (query: string) => void` for persisting search across tab switches.
- Implement in the create() body: `activeExplorerTab: 'library'`, `setActiveExplorerTab: (tab) => set({ activeExplorerTab: tab })`, `searchQuery: ''`, `setSearchQuery: (query) => set({ searchQuery: query })`.
  </action>
  <verify>
Run `npx tsc --noEmit` from ios/ to confirm no TypeScript errors. Verify ContentFilters now has `search` field and useContentList appends it to params.
  </verify>
  <done>
SearchInput renders a glass-styled search bar with icon and clear button. SuggestionCard is ready for Phase 15 data. useDebouncedValue provides generic debounce. useContentList now passes `search` parameter to backend. contentStore has activeExplorerTab and searchQuery state. No TS errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Explorer screen (library.tsx) with Suggestions + Library tabs and search</name>
  <files>
    ios/app/(tabs)/library.tsx
  </files>
  <action>
Rewrite `ios/app/(tabs)/library.tsx` completely. The new Explorer screen has this structure:

**Two-level tab architecture:**

1. **Top-level tabs**: "Suggestions" | "Bibliotheque" (using accent underline indicator, same style pattern as current tab bar but with colors.accent for the active indicator instead of colors.text).
   - Read/write `activeExplorerTab` from `useContentStore()`.

2. **Suggestions tab content**: Show `<EmptyState message="Des suggestions personnalisees arrivent bientot" icon={Sparkles} hasHeader />`. This is an honest placeholder until Phase 15 delivers GET /api/themes/suggestions.

3. **Library tab content**: This is a restructured version of the CURRENT library.tsx. It preserves ALL existing functionality:
   - **Search bar**: `<SearchInput>` at the top of Library tab content. Bind to `searchQuery` from contentStore. Use `useDebouncedValue(searchQuery, 300)` and pass `debouncedSearch` as `search` filter to `useContentList`.
   - **Collection/Triage sub-toggle**: Same as current (Ma collection | A trier), same underline style, same Badge for inbox count. Use `activeLibraryTab` from contentStore (already exists).
   - **Collection view**: FilterBar + content grid (existing ContentCard in 2-column grid). Pass `search: debouncedSearch || undefined` to the filters object alongside source, topic, channel.
   - **Triage view**: SourcePills + inbox items with selection mode + SelectionBar. Same batch triage logic (handleBatchLearn, handleBatchIgnore, etc.).
   - **All existing imports preserved**: ContentCard, FilterBar, SourcePills, SelectionBar from `../../components/content`.

**Key structural difference from current:**
- Current: `[Collection Tab] [Triage Tab]` at the top.
- New: `[Suggestions Tab] [Library Tab]` at the top, and inside Library tab: `[Collection] [Triage]` sub-toggle below SearchInput.
- The Library sub-toggle should be visually lighter than the top-level tabs (smaller text, no border-bottom on the container -- use a simple row with underline indicators).

**Visual hierarchy (top to bottom when Library tab active):**
1. Top tabs: Suggestions | Bibliotheque (with accent underline)
2. SearchInput (glass search bar)
3. Sub-tabs: Ma collection | A trier (with badge)
4. Filters (FilterBar for collection, SourcePills for triage)
5. Content grid / inbox items

**Styles:**
- Container: flex: 1, backgroundColor: colors.background.
- Top tabs: paddingHorizontal: spacing.lg, paddingTop: spacing.sm, gap: spacing.lg, borderBottomWidth: 1, borderBottomColor: colors.borderLight. Active indicator: colors.accent (not colors.text).
- Search input: marginHorizontal: spacing.lg, marginVertical: spacing.sm.
- Sub-tabs: same style as current tab bar but slightly smaller (variant="caption" instead of "body").
- ScrollView with paddingBottom: tabBarHeight + spacing.lg.

**Preserved behavior:**
- Pull-to-refresh on ScrollView invalidates content + inbox queries.
- Selection mode with SelectionBar at bottom for batch triage.
- Client-side inbox source filtering.
- useContentStore for all filter persistence.
  </action>
  <verify>
Run `npx tsc --noEmit` from ios/. Verify library.tsx has top-level Suggestions/Library tabs, search bar wired to useContentList via debouncedSearch, and Collection/Triage sub-toggle inside Library tab. Verify batch triage (SelectionBar) logic is preserved.
  </verify>
  <done>
Explorer screen has Suggestions tab with placeholder EmptyState, Library tab with search bar + Collection/Triage sub-toggle. Search queries the backend via `?search=` parameter through useContentList. All triage functionality (batch select, learn, archive) preserved. No TypeScript errors.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` from ios/ passes with zero errors
2. library.tsx has two top-level tabs (Suggestions, Bibliotheque) with accent-colored underline
3. Suggestions tab shows EmptyState with Sparkles icon and placeholder message
4. Library tab has SearchInput at top, Collection/Triage sub-toggle below
5. Search input is debounced (300ms) and passed to useContentList as `search` filter
6. Backend receives `?search=` parameter (verified in useContent.ts)
7. Batch triage (SelectionBar, learn/archive) fully functional in Triage sub-tab
8. FilterBar and SourcePills preserved in their respective sub-tabs
9. contentStore has activeExplorerTab and searchQuery state
10. Pull-to-refresh works on Library tab ScrollView
</verification>

<success_criteria>
- Explorer has a Suggestions tab displaying placeholder "coming soon" EmptyState
- Explorer has a Library tab with full-text search, source/category filters, and content grid
- Library tab preserves Collection/Triage sub-toggle with batch triage functionality
- Search queries backend via existing GET /api/content?search= endpoint
- Inbox count badge visible on Triage sub-tab
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-screen-rebuild/14-02-SUMMARY.md`
</output>
