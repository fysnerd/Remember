---
phase: 14-screen-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/components/home/GreetingHeader.tsx
  - ios/components/home/DailyThemeCard.tsx
  - ios/hooks/useDailyThemes.ts
  - ios/app/(tabs)/index.tsx
autonomous: true

must_haves:
  truths:
    - "Home screen displays a personalized greeting with time-of-day variant and user name (fallback to email prefix)"
    - "Home screen shows review stats (due today count, streak) in a compact header section"
    - "Home screen shows 3 daily theme cards in GlassCard style with emoji, title, content count, and question count"
    - "Discovery banner still appears when pending themes exist"
    - "Pull-to-refresh reloads all data on the home screen"
  artifacts:
    - path: "ios/components/home/GreetingHeader.tsx"
      provides: "Time-of-day greeting with user name + review stats summary"
      min_lines: 30
    - path: "ios/components/home/DailyThemeCard.tsx"
      provides: "Glass-styled theme card showing emoji, name, content count, question count, due badge"
      min_lines: 25
    - path: "ios/hooks/useDailyThemes.ts"
      provides: "Stub hook wrapping useThemes(), sorting by dueCards desc, taking first 3"
      exports: ["useDailyThemes"]
    - path: "ios/app/(tabs)/index.tsx"
      provides: "Rebuilt Home screen composing GreetingHeader + DailyThemeCard + discovery banner"
      min_lines: 80
  key_links:
    - from: "ios/hooks/useDailyThemes.ts"
      to: "ios/hooks/useThemes.ts"
      via: "wraps useThemes() + sort/slice"
      pattern: "useThemes\\(\\)"
    - from: "ios/app/(tabs)/index.tsx"
      to: "ios/hooks/useDailyThemes.ts"
      via: "import useDailyThemes"
      pattern: "useDailyThemes"
    - from: "ios/app/(tabs)/index.tsx"
      to: "ios/hooks/useReviews.ts"
      via: "import useReviewStats"
      pattern: "useReviewStats"
    - from: "ios/components/home/DailyThemeCard.tsx"
      to: "ios/components/glass/GlassCard.tsx"
      via: "import GlassCard"
      pattern: "GlassCard"
---

<objective>
Rebuild the Home screen (index.tsx) from a 2-column theme grid into a focused daily learning experience with personalized greeting, review stats, and 3 daily theme cards in Glass UI.

Purpose: The Home screen is the first thing users see -- it must communicate "here's what to learn today" instead of dumping all themes in a grid. The 3 daily themes (sorted by urgency) focus the user's attention and align with the Phase 15 daily rotation intent.

Output: Rebuilt Home screen with GreetingHeader component, DailyThemeCard component, useDailyThemes stub hook
</objective>

<execution_context>
@C:/Users/vmsan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/vmsan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-screen-rebuild/14-RESEARCH.md
@ios/theme.ts
@ios/components/glass/GlassCard.tsx
@ios/components/EmptyState.tsx
@ios/hooks/useThemes.ts
@ios/hooks/useReviews.ts
@ios/stores/authStore.ts
@ios/types/content.ts
@ios/app/(tabs)/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GreetingHeader, DailyThemeCard components and useDailyThemes hook</name>
  <files>
    ios/components/home/GreetingHeader.tsx
    ios/components/home/DailyThemeCard.tsx
    ios/hooks/useDailyThemes.ts
  </files>
  <action>
Create 3 new files:

**ios/hooks/useDailyThemes.ts** -- Stub hook that wraps `useThemes()`:
- Import and call `useThemes()` to get all discovered themes.
- Derive `dailyThemes` by sorting the themes array by `dueCards` descending (most urgent first), then by `updatedAt` descending as tiebreaker, then `.slice(0, 3)`.
- Return `{ data: dailyThemes, isLoading, error }` matching React Query hook shape.
- Add a `// TODO: Replace with GET /api/themes/daily when Phase 15 ships` comment at the top of the hook.
- Export `useDailyThemes` from this file AND add it to the barrel export in `ios/hooks/index.ts`.

**ios/components/home/GreetingHeader.tsx** -- Greeting + stats:
- Props: `userName?: string`, `stats?: { todayCount: number; streak: number }`.
- Compute greeting based on current hour: <12 "Bonjour", 12-17 "Bon apres-midi", >=18 "Bonsoir".
- Display: `{greeting}, {userName || 'there'}` as variant="h2".
- Below greeting: show stats in a row -- "{stats.todayCount} revisions aujourd'hui" and "Serie: {stats.streak} jours" using Text variant="caption" color="secondary". If stats is undefined, don't render stats row.
- Use spacing tokens from theme.ts (marginBottom: spacing.xl on container).
- No Glass component needed here -- it's just text.

**ios/components/home/DailyThemeCard.tsx** -- Theme card using GlassCard:
- Props: `theme: ThemeListItem`, `onPress: () => void`.
- Render a GlassCard with padding="lg" and onPress.
- Layout: Row with emoji (fontSize 32) on left, then column with theme name (Text variant="h3") and subtitle showing "{contentCount} contenus · {totalCards} questions".
- If `theme.dueCards > 0`, show a small badge at top-right corner: Text variant="caption" with "{dueCards} a revoir" in colors.accent.
- Import ThemeListItem from `../../types/content`.
- Use colors, spacing tokens from theme.ts.
  </action>
  <verify>
Run `npx tsc --noEmit` from the ios/ directory to confirm no TypeScript errors in the 3 new files. Verify the barrel export in hooks/index.ts includes useDailyThemes.
  </verify>
  <done>
Three new files exist: useDailyThemes.ts returns top 3 themes sorted by dueCards desc, GreetingHeader renders time-of-day greeting with stats, DailyThemeCard renders GlassCard-based theme card. No TS errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Home screen (index.tsx) using new components</name>
  <files>
    ios/app/(tabs)/index.tsx
    ios/hooks/index.ts
  </files>
  <action>
Rewrite `ios/app/(tabs)/index.tsx` completely. The new Home screen has this structure:

1. **Imports**: Import GreetingHeader and DailyThemeCard from `../../components/home/`, useDailyThemes from `../../hooks`, useReviewStats from `../../hooks`, useAuthStore from `../../stores/authStore`, usePendingThemes from `../../hooks`. Import useBottomTabBarHeight, ScrollView, RefreshControl, View from RN. Import ChevronRight from lucide-react-native. Import EmptyState, Link2 from existing.

2. **Data hooks**:
   - `const { user } = useAuthStore()` for greeting name.
   - `const { data: dailyThemes, isLoading: themesLoading } = useDailyThemes()`.
   - `const { data: stats } = useReviewStats()`.
   - `const { data: pendingThemes } = usePendingThemes()`.
   - `const tabBarHeight = useBottomTabBarHeight()`.

3. **userName derivation**: `const userName = user?.name || user?.email?.split('@')[0] || 'there'` (Pitfall 5 avoidance).

4. **Layout (top to bottom)**:
   - ScrollView with RefreshControl (invalidate queries: themes, reviews/stats).
   - `<GreetingHeader userName={userName} stats={stats} />`.
   - Discovery banner (same Pressable pattern as current -- keep it, but use GlassCard instead of the current Card with accent border). Show only if `pendingCount > 0`.
   - Section title "Themes du jour" (Text variant="h3").
   - Map `dailyThemes` into `<DailyThemeCard>` components with `gap: spacing.md` between them.
   - If dailyThemes is empty and themes are not loading, show EmptyState with message "Connectez vos plateformes pour commencer" and icon={Link2}.

5. **Loading state**: Show `<LoadingScreen />` if themesLoading.

6. **Styles**: Use colors.background for container, spacing.lg padding, paddingBottom: tabBarHeight + spacing.lg.

7. **Navigation**: handleThemePress navigates to `/theme/[id]` same as current. Discovery banner navigates to `/theme-discovery`.

**Key changes from current index.tsx:**
- REMOVE: 2-column grid layout, COLUMN_WIDTH calculations, suggestions section, contentList query, "New Theme" card.
- ADD: GreetingHeader, DailyThemeCard, useDailyThemes, useReviewStats.
- KEEP: Discovery banner (restyle with GlassCard), pull-to-refresh, paddingBottom for tab bar.
  </action>
  <verify>
Run `npx tsc --noEmit` from the ios/ directory. The screen should compile without errors. Verify the old 2-column grid code is removed and replaced with the new vertical daily themes layout.
  </verify>
  <done>
Home screen shows personalized greeting with time-of-day message, review stats (due today + streak), discovery banner if pending themes exist, and 3 daily theme GlassCards sorted by dueCards desc. No 2-column grid remains. TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` from ios/ passes with zero errors
2. index.tsx imports and uses GreetingHeader, DailyThemeCard, useDailyThemes, useReviewStats
3. useDailyThemes wraps useThemes() and returns max 3 items sorted by dueCards desc
4. GreetingHeader shows time-based greeting with fallback for missing user name
5. DailyThemeCard uses GlassCard (not old Card component) with theme data
6. Discovery banner preserved for pending themes
7. Pull-to-refresh invalidates themes and review stats queries
</verification>

<success_criteria>
- Home screen renders 3 daily theme GlassCards with emoji, title, content count, and question count
- Personalized greeting displays with correct time-of-day variant
- Review stats (due today, streak) visible below greeting
- Discovery banner appears when pending themes > 0
- Empty state shown when no themes exist
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-screen-rebuild/14-01-SUMMARY.md`
</output>
