---
phase: 14-screen-rebuild
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/components/reviews/RevisionCard.tsx
  - ios/components/reviews/CategoryChips.tsx
  - ios/app/(tabs)/reviews.tsx
  - ios/app/(tabs)/profile.tsx
autonomous: true

must_haves:
  truths:
    - "Revisions screen shows revision cards in GlassCard style with platform icon, title, and memo link"
    - "Revisions screen has category filter chips (platform-based) for filtering displayed items"
    - "Revisions screen has a search bar for full-text search across revision items"
    - "Profile screen shows user info with avatar placeholder, name/email, and plan"
    - "Profile screen displays connected platforms with connect/disconnect actions"
    - "Profile screen has settings section with logout"
    - "Profile screen user card uses GlassCard instead of old Card"
  artifacts:
    - path: "ios/components/reviews/RevisionCard.tsx"
      provides: "GlassCard-based revision item showing platform icon, title, and action"
      min_lines: 25
    - path: "ios/components/reviews/CategoryChips.tsx"
      provides: "Horizontal scrollable platform filter chips with active state"
      min_lines: 30
    - path: "ios/app/(tabs)/reviews.tsx"
      provides: "Rebuilt Revisions screen with filter chips, search, and GlassCard items"
      min_lines: 80
    - path: "ios/app/(tabs)/profile.tsx"
      provides: "Rebuilt Profile screen with GlassCard user info, platforms, and settings"
      min_lines: 100
  key_links:
    - from: "ios/app/(tabs)/reviews.tsx"
      to: "ios/components/reviews/CategoryChips.tsx"
      via: "import CategoryChips"
      pattern: "CategoryChips"
    - from: "ios/app/(tabs)/reviews.tsx"
      to: "ios/components/reviews/RevisionCard.tsx"
      via: "import RevisionCard"
      pattern: "RevisionCard"
    - from: "ios/app/(tabs)/reviews.tsx"
      to: "ios/components/explorer/SearchInput.tsx"
      via: "import SearchInput (shared component)"
      pattern: "SearchInput"
    - from: "ios/app/(tabs)/profile.tsx"
      to: "ios/components/glass/GlassCard.tsx"
      via: "import GlassCard for user card and platform list"
      pattern: "GlassCard"
---

<objective>
Rebuild the Revisions screen (reviews.tsx) with category filter chips, full-text search, and GlassCard-based revision cards, plus rebuild the Profile screen (profile.tsx) with GlassCard-based user info and settings sections.

Purpose: Revisions needs filter/search to handle growing revision libraries as users accumulate content. Profile needs visual upgrade to Glass UI and should display user name (not just email) as the primary identity.

Output: Rebuilt reviews.tsx with RevisionCard, CategoryChips and search; rebuilt profile.tsx with GlassCard user info and settings
</objective>

<execution_context>
@C:/Users/vmsan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/vmsan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-screen-rebuild/14-RESEARCH.md
@ios/theme.ts
@ios/components/glass/GlassCard.tsx
@ios/components/icons/PlatformIcon.tsx
@ios/components/EmptyState.tsx
@ios/hooks/useReviews.ts
@ios/hooks/useContent.ts
@ios/stores/authStore.ts
@ios/app/(tabs)/reviews.tsx
@ios/app/(tabs)/profile.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RevisionCard and CategoryChips, then rebuild Revisions screen</name>
  <files>
    ios/components/reviews/RevisionCard.tsx
    ios/components/reviews/CategoryChips.tsx
    ios/app/(tabs)/reviews.tsx
  </files>
  <action>
Create 2 new components and rewrite the Revisions screen:

**ios/components/reviews/RevisionCard.tsx** -- GlassCard revision item:
- Props: `title: string`, `subtitle: string`, `platform?: string`, `onPress: () => void`.
- Renders GlassCard with padding="md" and onPress.
- Layout: Row with left icon (PlatformIcon if platform provided, else BookOpen from Lucide), middle column with title (Text variant="body" weight="medium", numberOfLines: 2) and subtitle (Text variant="caption" color="secondary"), right ChevronRight icon (size 18, color textSecondary).
- Import PlatformIcon from `../icons`, GlassCard from `../glass`.

**ios/components/reviews/CategoryChips.tsx** -- Platform filter chips:
- Props: `selected: string` (active filter value, 'all' for none), `onSelect: (value: string) => void`.
- Render a horizontal ScrollView (horizontal, showsHorizontalScrollIndicator: false) with filter chips.
- Chips: "Tout" (value: 'all'), "YouTube", "Spotify", "TikTok", "Instagram".
- Each chip: Pressable with borderRadius.full, paddingVertical: spacing.xs, paddingHorizontal: spacing.md.
- Active chip: backgroundColor: colors.accent, text color: colors.background.
- Inactive chip: backgroundColor: colors.surface, borderWidth: 1, borderColor: colors.border, text color: colors.textSecondary.
- Use PlatformIcon (size 14) before the platform name text for platform-specific chips.
- Container: paddingHorizontal: spacing.lg, marginBottom: spacing.sm.

**Rewrite ios/app/(tabs)/reviews.tsx** -- Full Revisions screen:

The screen structure (top to bottom):
1. **Search bar**: Import and use `SearchInput` from `../../components/explorer/SearchInput` (shared component from Plan 14-02). Bind to local `useState` for searchText. Use `useDebouncedValue(searchText, 300)` for debounced filtering.
2. **CategoryChips**: Below search bar. Track `selectedCategory` in local useState (default: 'all').
3. **Revision list**: ScrollView with RefreshControl.

**Data flow:**
- Use `useCompletedItems()` for the base data (items + topics).
- Apply CLIENT-SIDE filtering (the /reviews endpoint doesn't support server-side search/filter):
  - Filter by category: if selectedCategory !== 'all', filter items where `item.source === selectedCategory`.
  - Filter by search: if debouncedSearch is not empty, filter items where `item.contentTitle.toLowerCase().includes(debouncedSearch.toLowerCase())` OR topics where `topic.name.toLowerCase().includes(debouncedSearch.toLowerCase())`.
- Render filtered topics first (using RevisionCard with subtitle "{contentCount} contenus · Theme", no platform icon, onPress navigates to `/memo/topic/[name]`).
- Then filtered content items (using RevisionCard with subtitle "Voir le memo", platform prop from item.source, onPress navigates to `/memo/[contentId]`).

**Key differences from current reviews.tsx:**
- ADD: SearchInput, CategoryChips, client-side filtering, RefreshControl.
- REPLACE: Card components with RevisionCard (GlassCard-based).
- KEEP: Same navigation patterns (handleContentMemo, handleTopicMemo).
- KEEP: EmptyState for no items.
- ADD: A second EmptyState variant when filters return nothing: "Aucun resultat pour cette recherche" with Search icon.

**Styles:**
- backgroundColor: colors.background, paddingBottom: tabBarHeight + spacing.lg.
- SearchInput: marginHorizontal: spacing.lg, marginTop: spacing.sm.
- List gap: spacing.md between cards.
- Padding: spacing.lg on the ScrollView content.

**NOTE:** This task depends on SearchInput from Plan 14-02. If 14-02 hasn't been executed yet, create a minimal inline SearchInput placeholder (TextInput with glass border styling) and add a TODO comment to replace it when 14-02 ships. The plans run in parallel (Wave 1), so handle this gracefully.
  </action>
  <verify>
Run `npx tsc --noEmit` from ios/. Verify reviews.tsx has SearchInput, CategoryChips, client-side filter logic, and RevisionCard components. Verify filtering works for both platform category and search text.
  </verify>
  <done>
Revisions screen has a search bar, platform category filter chips, and GlassCard-based revision cards. Client-side filtering applies to both topics and content items. Empty results state shown when filters match nothing. Pull-to-refresh reloads data. No TS errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebuild Profile screen with GlassCard and settings</name>
  <files>
    ios/app/(tabs)/profile.tsx
  </files>
  <action>
Rewrite `ios/app/(tabs)/profile.tsx` with Glass UI components. The structure:

**User info section** (top):
- Replace `<Card>` with `<GlassCard padding="lg">` for the user card.
- Avatar: Same circular placeholder (56x56, backgroundColor: colors.surfaceElevated) with User icon from Lucide.
- Name display: Show `user?.name || user?.email?.split('@')[0] || 'Utilisateur'` as primary text (Text variant="body" weight="medium"). This uses the same fallback chain as Home screen.
- Email: Show `user?.email` as secondary text (Text variant="caption" color="secondary").
- Plan: Show `user?.plan || 'FREE'` as a small Badge or text below email.

**Connected platforms section**:
- Section title: "Plateformes connectees" (Text variant="h3").
- Replace `<Card padding="none">` wrapper with `<GlassCard padding="none">`.
- Platform rows: Same Pressable rows with PlatformIcon, platform name, connect/disconnect text. Keep ALL existing connect/disconnect logic (handleConnect, handleDisconnect, handlePlatformPress) -- copy verbatim from current profile.tsx.
- Platform row borders: use glass.border color instead of colors.border for the separator.

**Settings section**:
- Section title: "Parametres" (Text variant="h3").
- Replace `<Card padding="none">` with `<GlassCard padding="none">`.
- Keep "A propos" row and "Deconnexion" row.
- Deconnexion text: colors.error as current.
- Add chevron icon (ChevronRight, size 16, color textSecondary) on the "A propos" row instead of the text "›".

**Dev Tools section** (keep for now):
- Replace `<Card padding="md">` with `<GlassCard padding="md">`.
- Keep Foundation Test button and sync grid exactly as-is.
- Replace the emoji in the section title (current has an emoji "🛠️") with a Wrench icon from Lucide (size 16, color error) since this is a debug section.

**Preserved behavior (copy from current profile.tsx):**
- ALL OAuth logic: handleConnect, handleDisconnect, handlePlatformPress, handleSync.
- WebBrowser OAuth flow for YouTube/Spotify.
- Router navigation to `/oauth/[platform]` for TikTok/Instagram.
- useOAuthStatus hook.
- queryClient invalidation after connect/disconnect.
- syncingPlatform and loadingPlatform state.
- Alert dialogs for disconnect confirmation and sync feedback.

**Styles:**
- Same layout as current but with glass.border for separators.
- paddingBottom: tabBarHeight + spacing.lg.
- All Card → GlassCard swaps.
  </action>
  <verify>
Run `npx tsc --noEmit` from ios/. Verify profile.tsx uses GlassCard instead of Card for all sections. Verify OAuth connect/disconnect logic is fully preserved (compare against current implementation). Verify user name fallback chain works (name > email prefix > 'Utilisateur').
  </verify>
  <done>
Profile screen renders user info in GlassCard with name (fallback to email prefix), connected platforms in GlassCard with full OAuth functionality preserved, settings section in GlassCard with logout action, and dev tools in GlassCard. All connect/disconnect/sync logic identical to before. No TypeScript errors.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` from ios/ passes with zero errors
2. reviews.tsx has SearchInput, CategoryChips, and RevisionCard components
3. reviews.tsx filters items client-side by platform category and search text
4. profile.tsx uses GlassCard for all sections (user info, platforms, settings, dev tools)
5. profile.tsx preserves all OAuth connect/disconnect/sync logic verbatim
6. RevisionCard uses GlassCard with PlatformIcon and navigation
7. CategoryChips has "Tout" + 4 platform filter chips with active accent state
8. Both screens use useBottomTabBarHeight for paddingBottom
9. Pull-to-refresh works on reviews.tsx
</verification>

<success_criteria>
- Revisions screen shows revision cards with category filter chips and full-text search
- Category chips filter by platform (YouTube, Spotify, TikTok, Instagram) or show all
- Search filters revision items by title (client-side)
- Profile screen displays user name (with email prefix fallback) and plan in GlassCard
- Profile screen has platform connect/disconnect with full OAuth flow
- Profile screen has settings section with logout
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-screen-rebuild/14-03-SUMMARY.md`
</output>
