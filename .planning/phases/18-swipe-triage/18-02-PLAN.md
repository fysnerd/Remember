---
phase: 18-swipe-triage
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - ios/app/(tabs)/library.tsx
autonomous: true

must_haves:
  truths:
    - "Bibliotheque tab defaults to swipe mode showing SwipeCardStack"
    - "User can tap TriageModeToggle in top-right to switch to bulk select grid"
    - "In bulk mode, user can select items, batch learn or archive via SelectionBar"
    - "SourcePills filter works in both swipe and bulk modes"
    - "Pull-to-refresh triggers platform sync and refreshes inbox data"
    - "Inbox items are sorted most recent first (capturedAt desc, handled server-side)"
    - "Switching filter resets swipe card index to 0"
    - "Pre-fetch next page when 5 cards remain in swipe mode"
    - "Celebratory empty state shows when all cards are swiped"
  artifacts:
    - path: "ios/app/(tabs)/library.tsx"
      provides: "Integrated swipe + bulk triage modes in Bibliotheque tab"
      contains: "SwipeCardStack"
  key_links:
    - from: "ios/app/(tabs)/library.tsx"
      to: "ios/components/content/SwipeCardStack.tsx"
      via: "SwipeCardStack component receiving inbox items"
      pattern: "SwipeCardStack"
    - from: "ios/app/(tabs)/library.tsx"
      to: "ios/hooks/useSwipeTriage.ts"
      via: "swipeTriage.mutate on swipe right/left"
      pattern: "useSwipeTriage"
    - from: "ios/app/(tabs)/library.tsx"
      to: "ios/stores/contentStore.ts"
      via: "triageMode state driving which UI renders"
      pattern: "triageMode"
---

<objective>
Integrate the swipe card stack into the library screen's Bibliotheque tab, making swipe mode the default triage experience with bulk select as a toggle-able secondary mode. Wire up SourcePills filtering, pull-to-refresh sync, pagination pre-fetch, and proper empty states for both modes.

Purpose: Connect the swipe infrastructure from Plan 18-01 into the actual user-facing triage flow, completing all 8 TRIAGE requirements.
Output: Fully refactored library.tsx with dual-mode triage (swipe default + bulk toggle).
</objective>

<execution_context>
@C:/Users/vmsan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/vmsan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-swipe-triage/18-RESEARCH.md
@.planning/phases/18-swipe-triage/18-01-SUMMARY.md

@ios/app/(tabs)/library.tsx
@ios/components/content/SwipeCardStack.tsx
@ios/components/content/TriageModeToggle.tsx
@ios/components/content/SourcePills.tsx
@ios/components/content/SelectionBar.tsx
@ios/hooks/useSwipeTriage.ts
@ios/hooks/useInbox.ts
@ios/stores/contentStore.ts
@ios/lib/haptics.ts
@ios/theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor Bibliotheque tab to swipe mode as default with TriageModeToggle</name>
  <files>
    ios/app/(tabs)/library.tsx
  </files>
  <action>
    Refactor the `renderLibraryTab()` function in library.tsx to support dual triage modes controlled by `triageMode` from contentStore.

    **New imports to add:**
    - `SwipeCardStack`, `TriageModeToggle` from `../../components/content`
    - `useSwipeTriage` from `../../hooks`
    - `useContentStore` already imported -- destructure `triageMode`, `setTriageMode` from it
    - `PartyPopper` from `lucide-react-native` (for celebratory empty state)

    **Store state changes:**
    - Destructure `triageMode` and `setTriageMode` from `useContentStore()` (alongside existing activeExplorerTab, searchQuery, etc.)

    **Add swipe triage hook:**
    - `const swipeTriage = useSwipeTriage();`

    **Swipe mode handlers:**
    - `handleSwipeRight = (item: Content) => { swipeTriage.mutate({ contentId: item.id, action: 'learn' }); showToast('Contenu sauvegarde', 'success'); }`
    - `handleSwipeLeft = (item: Content) => { swipeTriage.mutate({ contentId: item.id, action: 'archive' }); }`
    - `handleSwipeEmpty = () => {}` -- no-op, the empty state is handled by checking filtered items length
    - `handleNearEnd = () => { if (hasNextPage && !isFetchingNextPage) fetchNextPage(); }` -- pre-fetch next page

    **Filter reset on source change:**
    - Override `setSourceFilter` to also reset the SwipeCardStack's internal index. The simplest approach: When sourceFilter changes, the items array changes (useInbox re-fetches), and SwipeCardStack receives a new items array. The SwipeCardStack should already reset its currentIndex when items change (if items[currentIndex] no longer exists). To be safe, add a `key={sourceFilter}` prop to `<SwipeCardStack>` to force remount on filter change. This ensures currentIndex resets to 0.

    **Refactor `renderLibraryTab()`:**
    Replace the current single-mode rendering with a conditional based on `triageMode`:

    ```
    if (triageMode === 'swipe') {
      // SWIPE MODE:
      // 1. SourcePills at top (same as now)
      // 2. SwipeCardStack in the center (NOT inside ScrollView!)
      // 3. If inboxLoading -> LoadingScreen
      // 4. If no items -> celebratory EmptyState wrapped in a View with RefreshControl-capable ScrollView
      // 5. If items exist -> SwipeCardStack with items, key={sourceFilter}
      //
      // Pull-to-refresh: Since SwipeCardStack must NOT be in a ScrollView (gesture conflict),
      // place a wrapping View. For pull-to-refresh, use a transparent ScrollView wrapper approach:
      // Actually, the simpler approach per research: add a dedicated refresh button or
      // wrap the entire swipe area in a simple View with a RefreshControl-capable parent.
      //
      // BEST APPROACH: Wrap the entire swipe mode content in a ScrollView with
      // contentContainerStyle={{ flex: 1 }} and bounces={true} and refreshControl.
      // The ScrollView is non-scrollable (flex: 1 fills it) but RefreshControl still works
      // on the pull-down bounce. The card stack sits inside this non-scrollable ScrollView.
      // This avoids gesture conflicts because the ScrollView has no scroll content to compete with.
    } else {
      // BULK MODE:
      // Keep existing FlatList-based grid with SearchInput, SourcePills, selection, SelectionBar.
      // This is essentially the current renderLibraryTab() content with minor cleanup.
      // Remove the search bar from bulk mode (swipe is for quick triage, no search needed there;
      // bulk mode retains search for finding specific items).
    }
    ```

    **Add TriageModeToggle to tab header area:**
    In the `topTabBar` View, when `activeExplorerTab === 'library'`, render `<TriageModeToggle mode={triageMode} onToggle={handleToggleMode} />` positioned at the right side of the tab bar.

    Create `handleToggleMode`:
    ```
    const handleToggleMode = () => {
      const newMode = triageMode === 'swipe' ? 'bulk' : 'swipe';
      setTriageMode(newMode);
      // Reset selection when switching to swipe mode
      if (newMode === 'swipe') {
        setSelectedIds(new Set());
      }
    };
    ```

    **Modify topTabBar layout:**
    - Change topTabBar to have `justifyContent: 'space-between'` with a left group (tab buttons) and a right group (toggle button, only when on library tab).
    - Wrap the two tab Pressables in a View with `flexDirection: 'row'` and `gap: spacing.lg`.
    - Conditionally render TriageModeToggle on the right side when `activeExplorerTab === 'library'`.

    **Swipe mode empty state:**
    When `triageMode === 'swipe'` and `filteredInboxItems?.length === 0` and not loading:
    - If `sourceFilter !== 'all'`: show "Aucun contenu [platform] a trier" with Search icon
    - Else: show "Tout est trie !" with PartyPopper icon (celebratory)
    - Wrap in ScrollView with RefreshControl so user can pull-to-refresh even on empty state.

    **Styles to add/modify:**
    - `swipeContainer`: `{ flex: 1 }` -- wrapper for swipe mode
    - `swipeScrollWrapper`: `{ flex: 1 }` -- ScrollView contentContainer for pull-to-refresh
    - Modify `topTabBar` to use space-between layout with inner group
    - `topTabGroup`: `{ flexDirection: 'row', gap: spacing.lg }`

    **Preserve all existing bulk mode behavior:**
    The current FlatList grid with selection, batch actions, and SelectionBar must continue working identically when triageMode === 'bulk'. Do not break existing functionality.
  </action>
  <verify>
    Run `npx tsc --noEmit` from ios/ directory. Zero type errors. Verify library.tsx renders without crash by checking the import chain compiles.
  </verify>
  <done>
    Bibliotheque tab defaults to swipe mode with SwipeCardStack. Toggle button switches to bulk grid. SourcePills work in both modes. Pull-to-refresh works in swipe mode. Filter changes reset card index. Pre-fetch fires when 5 cards remain. Celebratory empty state when all swiped.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify full triage flow compiles and all requirements are wired</name>
  <files>
    ios/app/(tabs)/library.tsx
  </files>
  <action>
    Final verification pass on library.tsx to ensure all 8 TRIAGE requirements are met:

    **TRIAGE-01 (swipe right = keep):** Verify `handleSwipeRight` calls `swipeTriage.mutate({ contentId: item.id, action: 'learn' })`. Verify SwipeCardStack passes `onSwipeRight={handleSwipeRight}`.

    **TRIAGE-02 (swipe left = dismiss):** Verify `handleSwipeLeft` calls `swipeTriage.mutate({ contentId: item.id, action: 'archive' })`. Verify SwipeCardStack passes `onSwipeLeft={handleSwipeLeft}`.

    **TRIAGE-03 (toggle button):** Verify TriageModeToggle renders in tab bar when library tab active. Verify `handleToggleMode` toggles between 'swipe' and 'bulk'.

    **TRIAGE-04 (bulk select + batch):** Verify bulk mode renders FlatList with selection, SelectionBar with learn/ignore buttons, batch triage mutation.

    **TRIAGE-05 (source filter both modes):** Verify SourcePills renders in both swipe and bulk mode. Verify `sourceFilter` is passed to `useInbox(sourceFilter)`.

    **TRIAGE-06 (pull-to-refresh):** Verify ScrollView wrapper with RefreshControl exists in swipe mode. Verify `onRefresh` triggers platform sync + query invalidation.

    **TRIAGE-07 (capturedAt desc):** This is handled server-side by the GET /api/content/inbox endpoint which already sorts by capturedAt desc. Verify `useInbox` is used without any client-side re-sorting.

    **TRIAGE-08 (spring physics animation):** Handled by SwipeCard component in Plan 18-01. Verify SwipeCardStack is used (which uses SwipeCard internally).

    **If any requirement is not wired, add the missing code.**

    **Additional edge case checks:**
    - Ensure `key={sourceFilter}` is on SwipeCardStack to force remount on filter change
    - Ensure `handleNearEnd` is passed as `onNearEnd` prop to SwipeCardStack
    - Ensure toast feedback appears on swipe right ("Contenu sauvegarde")
    - Ensure `selectedIds` is cleared when toggling to swipe mode
    - Ensure the `tabBarHeight` padding is applied correctly in swipe mode container
  </action>
  <verify>
    Run `npx tsc --noEmit` from ios/ directory. Zero type errors. Manually trace each TRIAGE-XX requirement to a specific code path in library.tsx.
  </verify>
  <done>
    All 8 TRIAGE requirements have verified code paths. TypeScript compiles cleanly. The library screen supports dual-mode triage with swipe as default, bulk as toggle, filtering in both, pull-to-refresh, pagination, and proper empty states.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. TRIAGE-01: swipe right calls PATCH learn
3. TRIAGE-02: swipe left calls PATCH archive
4. TRIAGE-03: TriageModeToggle visible in library tab header
5. TRIAGE-04: bulk mode with FlatList + SelectionBar works as before
6. TRIAGE-05: SourcePills renders in both modes
7. TRIAGE-06: pull-to-refresh works in swipe mode via ScrollView wrapper
8. TRIAGE-07: server-side capturedAt desc sorting preserved
9. TRIAGE-08: spring physics via SwipeCard from 18-01
10. Filter change forces SwipeCardStack remount (key={sourceFilter})
11. Pre-fetch next page when 5 cards remain
12. Celebratory empty state when inbox is clear
</verification>

<success_criteria>
library.tsx compiles cleanly with dual-mode triage. All 8 TRIAGE requirements are satisfied. Swipe mode is the default. Bulk mode is toggle-able. Both modes support filtering and pull-to-refresh.
</success_criteria>

<output>
After completion, create `.planning/phases/18-swipe-triage/18-02-SUMMARY.md`
</output>
