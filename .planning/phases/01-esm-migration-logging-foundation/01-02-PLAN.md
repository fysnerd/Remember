---
phase: 01-esm-migration-logging-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend/src/workers/scheduler.ts
  - backend/src/workers/youtubeSync.ts
  - backend/src/workers/spotifySync.ts
  - backend/src/workers/tiktokSync.ts
  - backend/src/workers/instagramSync.ts
  - backend/src/workers/reminderWorker.ts
autonomous: true

must_haves:
  truths:
    - "Cron job execution can be traced end-to-end via structured logs (job name, status, duration)"
    - "Cron job overlap incidents are detectable from log output without grepping unstructured text"
    - "Failed cron jobs produce error logs with enough context (stack trace, job name) to diagnose the root cause"
    - "Operator can determine which jobs are active and their schedules from a single log entry at startup"
  artifacts:
    - path: "backend/src/workers/scheduler.ts"
      provides: "Structured logging for job orchestration"
      contains: "logger.child"
    - path: "backend/src/workers/youtubeSync.ts"
      provides: "Structured logging for YouTube sync"
      contains: "logger.child"
    - path: "backend/src/workers/spotifySync.ts"
      provides: "Structured logging for Spotify sync"
      contains: "logger.child"
    - path: "backend/src/workers/tiktokSync.ts"
      provides: "Structured logging for TikTok sync"
      contains: "logger.child"
    - path: "backend/src/workers/instagramSync.ts"
      provides: "Structured logging for Instagram sync"
      contains: "logger.child"
    - path: "backend/src/workers/reminderWorker.ts"
      provides: "Structured logging for reminder worker"
      contains: "logger.child"
  key_links:
    - from: "backend/src/workers/scheduler.ts"
      to: "backend/src/config/logger.ts"
      via: "imports logger for child loggers"
      pattern: "import.*logger.*from.*config/logger"
    - from: "backend/src/workers/youtubeSync.ts"
      to: "backend/src/config/logger.ts"
      via: "imports logger for child loggers"
      pattern: "import.*logger.*from.*config/logger"
---

<objective>
Replace all 127 console.log/error statements in the 6 worker files with structured Pino logging using child loggers.

Purpose: Workers are the most critical logging targets -- they run every 2-30 minutes and failures are currently invisible in unstructured logs. Structured logging with job name, duration, and error context enables the observability dashboard (Phase 4).
Output: All 6 worker files use Pino child loggers with structured context.
</objective>

<execution_context>
@C:/Users/vmsan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/vmsan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/.planning/PROJECT.md
@C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/.planning/ROADMAP.md
@C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/.planning/phases/01-esm-migration-logging-foundation/01-RESEARCH.md
@C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/.planning/phases/01-esm-migration-logging-foundation/01-01-SUMMARY.md
@C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/backend/src/workers/scheduler.ts
@C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/backend/src/workers/youtubeSync.ts
@C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/backend/src/workers/spotifySync.ts
@C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/backend/src/workers/tiktokSync.ts
@C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/backend/src/workers/instagramSync.ts
@C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/backend/src/workers/reminderWorker.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate scheduler.ts to Pino structured logging</name>
  <files>backend/src/workers/scheduler.ts</files>
  <action>
    This file has 29 console.log/error statements. It is the scheduler orchestrator.

    1. Add import: `import { logger } from '../config/logger.js';`
    2. Create scheduler child logger at module level: `const log = logger.child({ component: 'scheduler' });`

    3. In `runJob()` wrapper function:
       - Replace `console.log(\`[Scheduler] Skipping ${jobName}...\`)` with `log.warn({ job: jobName }, 'Skipping job - previous run still in progress');`
       - Replace `console.error(\`[Scheduler] Error in ${jobName}:\`, error)` with `log.error({ job: jobName, err: error }, 'Job execution failed');`
       - Add timing: capture `const startTime = Date.now()` before `await job()`, then `const duration = Date.now() - startTime;` after. Log on success: `log.info({ job: jobName, durationMs: duration }, 'Job completed');`

    4. In `startScheduler()`:
       - Replace `console.log('[Scheduler] Already running...')` with `log.warn('Scheduler already running, skipping initialization');`
       - Replace `console.log('[Scheduler] Initializing...')` with `log.info('Initializing cron jobs');`
       - For each cron.schedule callback: replace `console.log('[Scheduler] Running X...')` with `log.info({ job: 'x-name' }, 'Triggering scheduled job');`
       - Replace the final block of 11+ console.log lines listing all jobs with a single structured log:
         ```typescript
         log.info({
           jobs: [
             { name: 'youtube-sync', schedule: '*/15 * * * *' },
             { name: 'spotify-sync', schedule: '*/30 * * * *' },
             // ... all 11 jobs
           ]
         }, 'All cron jobs scheduled');
         ```

    5. In `runAllSyncsNow()`:
       - Replace `console.log('[Scheduler] Running all syncs now...')` with `log.info('Running all syncs now (manual trigger)');`
       - Replace `console.log('[Scheduler] All syncs completed')` with `log.info('All syncs completed');`

    6. Ensure ZERO console.log/error remain in this file.
  </action>
  <verify>
    Run `grep -c "console\." backend/src/workers/scheduler.ts` -- must return 0.
    Run `cd C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/backend && npx tsc --noEmit` -- must compile.
  </verify>
  <done>scheduler.ts has zero console.log statements. All logging is structured with job name, duration, and error context. Job overlap warnings use warn level. Job failures use error level with err serialization.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all 5 sync worker files to Pino</name>
  <files>backend/src/workers/youtubeSync.ts, backend/src/workers/spotifySync.ts, backend/src/workers/tiktokSync.ts, backend/src/workers/instagramSync.ts, backend/src/workers/reminderWorker.ts</files>
  <action>
    For EACH of the 5 worker files, apply this pattern:

    1. Add import: `import { logger } from '../config/logger.js';`
    2. Create a child logger at module level with the job name:
       - youtubeSync.ts: `const log = logger.child({ job: 'youtube-sync' });`
       - spotifySync.ts: `const log = logger.child({ job: 'spotify-sync' });`
       - tiktokSync.ts: `const log = logger.child({ job: 'tiktok-sync' });`
       - instagramSync.ts: `const log = logger.child({ job: 'instagram-sync' });`
       - reminderWorker.ts: `const log = logger.child({ job: 'reminder' });`

    3. Replace ALL console.log/error/warn statements following these rules:
       - `console.log('[Tag] Starting...')` -> `log.info('Starting sync');`
       - `console.log('[Tag] Found X users')` -> `log.info({ userCount: X }, 'Found users to sync');`
       - `console.log('[Tag] Processing user...')` -> `log.debug({ userId: user.id }, 'Processing user');`
       - `console.log('[Tag] Added N items')` -> `log.info({ count: N, userId: user.id }, 'New content synced');`
       - `console.error('[Tag] Error...', error)` -> `log.error({ err: error, userId: user.id }, 'Sync failed for user');`
       - `console.log('[Tag] Completed')` -> `log.info('Sync completed');`

    4. Key patterns per file:
       - **youtubeSync.ts** (8 occurrences): Focus on user sync loop, video count, API errors
       - **spotifySync.ts** (13 occurrences): Focus on episode processing, pagination, API errors
       - **tiktokSync.ts** (55 occurrences -- MOST DENSE): This file has extensive debug logging for Playwright browser automation, cookie handling, scrolling, video extraction. Convert to appropriate levels:
         - Browser launch/navigation -> `log.debug()`
         - Cookie loading -> `log.debug()`
         - Video extraction counts -> `log.info()`
         - Scroll progress -> `log.debug()`
         - Errors/timeouts -> `log.error()`
         - Success counts -> `log.info()`
       - **instagramSync.ts** (17 occurrences): Similar to tiktokSync -- Playwright automation logs. Same level mapping.
       - **reminderWorker.ts** (5 occurrences): Simple -- user reminder scheduling and email send status.

    5. Include structured context in ALL log calls:
       - Always include `userId` when processing a specific user
       - Always include counts (`videoCount`, `episodeCount`, `itemCount`) when reporting quantities
       - Always use `{ err: error }` (not `{ error }`) for Pino's error serializer
       - Use `log.debug()` for verbose operational detail, `log.info()` for business events, `log.error()` for failures

    6. Ensure ZERO console.log/error remain in any of these 5 files.
  </action>
  <verify>
    Run for each file:
    ```bash
    grep -c "console\." backend/src/workers/youtubeSync.ts
    grep -c "console\." backend/src/workers/spotifySync.ts
    grep -c "console\." backend/src/workers/tiktokSync.ts
    grep -c "console\." backend/src/workers/instagramSync.ts
    grep -c "console\." backend/src/workers/reminderWorker.ts
    ```
    All must return 0.
    Run `cd C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/backend && npx tsc --noEmit` -- must compile.
  </verify>
  <done>All 5 worker files (youtubeSync, spotifySync, tiktokSync, instagramSync, reminderWorker) use Pino child loggers with structured context. Zero console.log statements remain. TikTok's 55 verbose logs are appropriately leveled (debug for browser automation, info for business events, error for failures).</done>
</task>

</tasks>

<verification>
1. `grep -r "console\." backend/src/workers/` returns 0 matches
2. `cd C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/backend && npx tsc --noEmit` compiles successfully
3. Each worker file imports from `'../config/logger.js'`
4. Each worker file creates a child logger with `{ job: 'job-name' }`
</verification>

<success_criteria>
- All 127 console.log/error statements across 6 worker files replaced with Pino
- Each worker uses a child logger with job name context
- scheduler.ts logs job duration and overlap warnings
- tiktokSync.ts verbose logging uses debug level (not info)
- Zero console.log/error/warn remain in backend/src/workers/
</success_criteria>

<output>
After completion, create `.planning/phases/01-esm-migration-logging-foundation/01-02-SUMMARY.md`
</output>
