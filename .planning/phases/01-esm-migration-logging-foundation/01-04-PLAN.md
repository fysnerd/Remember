---
phase: 01-esm-migration-logging-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend/src/routes/oauth.ts
  - backend/src/routes/content.ts
  - backend/src/routes/auth.ts
  - backend/src/routes/admin.ts
  - backend/src/routes/export.ts
  - backend/src/services/tokenRefresh.ts
  - backend/src/services/export.ts
  - backend/src/config/database.ts
autonomous: true

must_haves:
  truths:
    - "All route logging is structured JSON with route name and relevant context"
    - "OAuth flow logs include platform and userId for debugging"
    - "No console.log statements remain anywhere in the backend src directory (except 2 in env.ts)"
    - "Backend compiles and builds cleanly as ESM with all Pino logging"
    - "Scheduler starts successfully and registers all 11 cron jobs at runtime (ESM-03)"
    - "PM2 cluster mode restarts cleanly with the ESM backend (ESM-05)"
  artifacts:
    - path: "backend/src/routes/oauth.ts"
      provides: "Structured logging for OAuth routes"
      contains: "logger.child"
    - path: "backend/src/routes/content.ts"
      provides: "Structured logging for content routes"
      contains: "logger.child"
    - path: "backend/src/routes/auth.ts"
      provides: "Structured logging for auth routes"
      contains: "logger.child"
  key_links:
    - from: "backend/src/routes/oauth.ts"
      to: "backend/src/config/logger.ts"
      via: "imports logger singleton"
      pattern: "import.*logger.*from.*config/logger"
    - from: "backend/src/workers/scheduler.ts"
      to: "backend/src/index.ts"
      via: "scheduler imported and started at boot"
      pattern: "startScheduler|import.*scheduler"
---

<objective>
Replace all remaining console.log/error statements in routes and any remaining files, then verify the entire backend is clean AND runs correctly at runtime -- including scheduler initialization and PM2 compatibility.

Purpose: Routes handle OAuth callbacks, content operations, and auth -- the final category of files with console.log. After this plan, zero console.log statements remain in the codebase (except 2 intentional ones in env.ts for bootstrap validation). This plan also covers the critical runtime verification that the ESM-migrated backend actually starts, registers all cron jobs, and works under PM2 cluster mode.
Output: Fully migrated codebase with zero unstructured logging. Backend compiles, builds, and runs as ESM -- verified at runtime level.
</objective>

<execution_context>
@C:/Users/vmsan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/vmsan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/.planning/PROJECT.md
@C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/.planning/ROADMAP.md
@C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/.planning/phases/01-esm-migration-logging-foundation/01-RESEARCH.md
@C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/.planning/phases/01-esm-migration-logging-foundation/01-01-SUMMARY.md
@C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/backend/src/routes/oauth.ts
@C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/backend/src/routes/content.ts
@C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/backend/src/routes/auth.ts
@C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/backend/src/routes/admin.ts
@C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/backend/src/routes/export.ts
@C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/backend/src/config/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate route files to Pino (5 files, 54 occurrences)</name>
  <files>backend/src/routes/oauth.ts, backend/src/routes/content.ts, backend/src/routes/auth.ts, backend/src/routes/admin.ts, backend/src/routes/export.ts</files>
  <action>
    For EACH route file:

    1. Add import: `import { logger } from '../config/logger.js';`
    2. Create child logger at module level:
       - oauth.ts: `const log = logger.child({ route: 'oauth' });`
       - content.ts: `const log = logger.child({ route: 'content' });`
       - auth.ts: `const log = logger.child({ route: 'auth' });`
       - admin.ts: `const log = logger.child({ route: 'admin' });`
       - export.ts: `const log = logger.child({ route: 'export' });`

    3. Per-file specifics:

       **oauth.ts** (23 occurrences -- second densest route):
       - Include `userId`, `platform` in structured context
       - OAuth connect initiation: `log.info({ userId, platform }, 'OAuth connect initiated');`
       - OAuth callback received: `log.info({ platform }, 'OAuth callback received');`
       - Token exchange success: `log.info({ userId, platform }, 'OAuth token exchange successful');`
       - Token exchange failure: `log.error({ err: error, platform }, 'OAuth token exchange failed');`
       - Cookie-based auth (TikTok/Instagram): `log.info({ userId, platform }, 'Cookie-based auth initiated');`

       **content.ts** (24 occurrences -- densest route):
       - Include `userId`, `contentId` where available
       - Content list/fetch: debug level (high frequency, pino-http already logs the request)
       - Content triage: `log.info({ userId, contentIds, action }, 'Bulk triage performed');`
       - Content operations: info level for mutations, debug for reads
       - Errors: `log.error({ err: error, userId }, 'Content operation failed');`

       **auth.ts** (5 occurrences):
       - Include `userId` (on success) or `email` (do NOT log passwords)
       - Login success: `log.info({ userId }, 'User logged in');`
       - Login failure: `log.warn({ email }, 'Login failed - invalid credentials');`
       - Signup: `log.info({ userId }, 'New user registered');`
       - IMPORTANT: NEVER log passwords, tokens, or JWT values.

       **admin.ts** (1 occurrence):
       - Replace the single console.log with appropriate Pino log.

       **export.ts** (1 occurrence):
       - Replace the single console.log with appropriate Pino log.

    4. Ensure ZERO console.log/error remain in each file.
  </action>
  <verify>
    Run `grep -rc "console\." backend/src/routes/` -- must return 0 for all files.
    Run `cd C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/backend && npx tsc --noEmit` -- must compile.
  </verify>
  <done>All 5 route files use Pino with structured context. OAuth routes include platform and userId. Auth routes never log passwords or tokens. Content routes include userId and contentId. Zero console.log statements remain in backend/src/routes/.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate remaining files + final codebase verification + runtime verification</name>
  <files>backend/src/services/tokenRefresh.ts, backend/src/config/database.ts</files>
  <action>
    **Part A: Remaining file migration**

    1. Check `backend/src/services/tokenRefresh.ts` for any console.log statements:
       - If found: add `import { logger } from '../config/logger.js';`, create child logger `const log = logger.child({ service: 'token-refresh' });`, replace all console.log/error.
       - If none: skip this file.

    2. Check `backend/src/config/database.ts` for any console.log statements:
       - If found: add `import { logger } from './logger.js';`, replace with logger calls.
       - Note: database.ts imports happen early in startup. If logger is available (env.ts has loaded), use it. If there's a chicken-and-egg issue (database.ts loads before logger.ts), handle similarly to env.ts -- leave as console.error or use process.stderr.write.
       - If none: skip this file.

    3. Run a COMPREHENSIVE search for any remaining console.log/error/warn in the entire src directory:
       ```bash
       grep -rn "console\." backend/src/ --include="*.ts"
       ```
       The ONLY acceptable matches should be in `backend/src/config/env.ts` (2 lines for bootstrap validation).

    4. If any other files have console.log statements, fix them following the same pattern (import logger, create child logger, replace).

    **Part B: Full build verification**

    5. Final full build:
       ```bash
       cd C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/backend
       npm run build
       ```

    6. Verify the built output resolves ESM modules correctly:
       ```bash
       cd C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/backend
       node -e "import('./dist/index.js').catch(e => { console.error(e.message); process.exit(1); })"
       ```
       Note: This will fail to fully start (no .env, no DB) but should not fail on module resolution. The import itself succeeding means ESM is working.

    **Part C: Scheduler runtime verification (ESM-03)**

    7. Verify the scheduler initializes and registers all 11 cron jobs. After deployment to VPS (or locally if .env is configured), check that the startup log contains the structured log entry listing all 11 jobs:
       ```bash
       # On VPS after deploy:
       ssh root@116.203.17.203 "cd /root/Remember/backend && git pull && npm run build && pm2 restart remember-api && sleep 5 && pm2 logs remember-api --lines 30 --nostream"
       ```
       Expected: Look for a log line containing `"All cron jobs scheduled"` with a `jobs` array of 11 entries. This confirms ESM-03 (scheduler starts and registers all jobs at runtime).

    **Part D: PM2 cluster mode verification (ESM-05)**

    8. After VPS deployment, verify PM2 restarts cleanly in cluster mode with the ESM backend:
       ```bash
       ssh root@116.203.17.203 "pm2 status"
       ```
       Expected: `remember-api` process(es) show status `online` (not `errored` or `stopped`).

       ```bash
       ssh root@116.203.17.203 "pm2 logs remember-api --lines 10 --nostream | grep -i error"
       ```
       Expected: No ESM-related errors (no `ERR_REQUIRE_ESM`, no `Cannot use import statement outside a module`, no `ERR_MODULE_NOT_FOUND`).

       If PM2 has issues with ESM, check and update the PM2 ecosystem config:
       - Ensure `ecosystem.config.cjs` (not .js -- PM2 config must be CommonJS even when the app is ESM) or that `pm2 start dist/index.js` is used directly.
       - If PM2 uses an ecosystem file, it may need `interpreter_args: "--experimental-modules"` or the script path must point to the compiled .js output (not .ts).
       - Document the working PM2 configuration in the SUMMARY.

    9. Final health check:
       ```bash
       ssh root@116.203.17.203 "curl -s https://api.ankora.study/health | head -1"
       ```
       Expected: 200 OK or JSON health response confirming the API is serving requests.
  </action>
  <verify>
    Run `grep -rn "console\." backend/src/ --include="*.ts" | grep -v "config/env.ts"` -- must return EMPTY (no matches).
    Run `cd C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/backend && npm run build` -- must exit 0.
    Run `ls backend/dist/index.js` -- must exist (compiled output).
    After VPS deploy: `ssh root@116.203.17.203 "pm2 logs remember-api --lines 30 --nostream"` must show scheduler initialization with 11 jobs and no ESM errors.
    After VPS deploy: `ssh root@116.203.17.203 "pm2 status"` must show `online` status.
  </verify>
  <done>Zero console.log statements in entire backend/src/ except 2 intentional lines in env.ts. Backend compiles as ESM. dist/ output is valid JavaScript. All 405 original console.log occurrences across 27 files have been replaced with structured Pino logging. Scheduler runtime verified -- all 11 cron jobs register at startup (ESM-03). PM2 cluster mode restarts cleanly with ESM backend, no module resolution errors (ESM-05).</done>
</task>

</tasks>

<verification>
1. `grep -rn "console\." backend/src/ --include="*.ts" | grep -v "config/env.ts"` returns EMPTY
2. `grep -c "console\." backend/src/config/env.ts` returns exactly 2 (intentional bootstrap)
3. `cd C:/Users/vmsan/Desktop/FREE/PB/VIBE/Remember/backend && npm run build` exits 0
4. `ls backend/dist/config/logger.js` exists
5. `ls backend/dist/middleware/httpLogger.js` exists
6. `backend/package.json` contains `"type": "module"`
7. VPS deploy: PM2 status shows `online`, no ESM errors in logs
8. VPS deploy: Scheduler logs show all 11 cron jobs registered
9. VPS deploy: `curl https://api.ankora.study/health` returns success
</verification>

<success_criteria>
- All 54 console.log statements in route files replaced with Pino
- All remaining files (tokenRefresh, database) migrated or confirmed clean
- Comprehensive grep shows zero console.log in entire src (except env.ts bootstrap)
- Backend compiles and builds cleanly as ESM module
- Scheduler starts and registers all 11 cron jobs at runtime (ESM-03 verified)
- PM2 cluster mode runs cleanly with ESM backend -- no ERR_REQUIRE_ESM or module errors (ESM-05 verified)
- Health endpoint responds after deployment
- Phase 1 requirements fully satisfied: ESM-01 through ESM-05, LOG-01 through LOG-04
</success_criteria>

<output>
After completion, create `.planning/phases/01-esm-migration-logging-foundation/01-04-SUMMARY.md`
</output>
