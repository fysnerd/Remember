---
phase: 13-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/theme.ts
  - ios/components/ui/Button.tsx
  - ios/components/ui/Card.tsx
  - ios/components/ui/Input.tsx
  - ios/components/ui/Badge.tsx
  - ios/components/ui/TopicChip.tsx
  - ios/components/ui/Skeleton.tsx
  - ios/components/ui/Toast.tsx
  - ios/components/quiz/QuestionCard.tsx
autonomous: true

must_haves:
  truths:
    - "theme.ts exports a `glass` object with border, fill, shadow, intensity, and tint tokens"
    - "Button component accepts a `style` prop without TypeScript errors"
    - "QuestionCard quiz options use Night Blue dark colors instead of light-mode hex values"
    - "All 8 UI primitives render with Night Blue styling and no light-mode remnants"
  artifacts:
    - path: "ios/theme.ts"
      provides: "Glass design tokens (border, fill, shadow, tint, intensity)"
      contains: "export const glass"
    - path: "ios/components/ui/Button.tsx"
      provides: "Button with style prop fix and Night Blue styling"
      contains: "style?: StyleProp<ViewStyle>"
    - path: "ios/components/quiz/QuestionCard.tsx"
      provides: "QuestionCard with Night Blue option colors"
      contains: "colors.success"
  key_links:
    - from: "ios/components/quiz/QuestionCard.tsx"
      to: "ios/theme.ts"
      via: "colors import"
      pattern: "colors\\.(success|error|surface|text)"
    - from: "ios/components/ui/Toast.tsx"
      to: "ios/theme.ts"
      via: "colors import"
      pattern: "colors\\."
---

<objective>
Add glass design tokens to theme.ts, fix Button.tsx style prop TypeScript bug, restyle all 8 UI primitives for Night Blue consistency, and convert QuestionCard's 10+ hardcoded light-mode colors to theme tokens.

Purpose: Establishes the token foundation and ensures all existing UI components render correctly in Night Blue dark mode before Glass and Icon components are built on top.
Output: Updated theme.ts with glass tokens, 8 restyled UI primitives, QuestionCard dark mode conversion.
</objective>

<execution_context>
@C:/Users/vmsan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/vmsan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-foundation-build/12-01-SUMMARY.md
@.planning/phases/13-design-system/13-RESEARCH.md

Key files to read before modifying:
@ios/theme.ts
@ios/components/ui/Button.tsx
@ios/components/ui/Card.tsx
@ios/components/ui/Input.tsx
@ios/components/ui/Badge.tsx
@ios/components/ui/TopicChip.tsx
@ios/components/ui/Skeleton.tsx
@ios/components/ui/Toast.tsx
@ios/components/quiz/QuestionCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add glass tokens to theme.ts and restyle UI primitives</name>
  <files>
    ios/theme.ts
    ios/components/ui/Button.tsx
    ios/components/ui/Card.tsx
    ios/components/ui/Input.tsx
    ios/components/ui/Badge.tsx
    ios/components/ui/TopicChip.tsx
    ios/components/ui/Skeleton.tsx
    ios/components/ui/Toast.tsx
  </files>
  <action>
    **theme.ts** — Add a `glass` export object below the existing `layout` export:
    ```typescript
    export const glass = {
      border: 'rgba(255, 255, 255, 0.08)',
      borderLight: 'rgba(255, 255, 255, 0.12)',
      borderFocused: 'rgba(212, 165, 116, 0.3)',
      fill: 'rgba(17, 24, 39, 0.6)',
      fillElevated: 'rgba(30, 41, 59, 0.5)',
      shadow: {
        shadowColor: '#000',
        shadowOffset: { width: 0, height: 4 },
        shadowOpacity: 0.15,
        shadowRadius: 12,
        elevation: 8,
      },
      intensity: 60,
      tint: 'systemThinMaterialDark' as const,
      tabBarTint: 'systemChromeMaterialDark' as const,
      tabBarIntensity: 80,
    } as const;
    ```

    **Button.tsx** — Fix the missing `style` prop:
    1. Add `import { StyleProp, ViewStyle } from 'react-native'` (add to existing import)
    2. Add `style?: StyleProp<ViewStyle>` to `ButtonProps` interface
    3. Destructure `style` in the function params
    4. Apply `style` in the Pressable style array after `variantStyle`: `style && style,`
    5. Add subtle border to secondary variant: `borderWidth: 1, borderColor: colors.border`
    6. Ensure borderRadius uses `borderRadius.md` (currently `sm` -- upgrade for a more modern look)

    **Card.tsx** — Add subtle border for depth on dark backgrounds:
    1. Add to base style: `borderWidth: 1, borderColor: colors.border`
    2. Keep existing backgroundColor as `colors.surface`

    **Input.tsx** — Already well-themed from Phase 12. Verify focused border uses `colors.accent` (it does). Add `fontFamily: fonts.regular` to input style for Geist rendering instead of system font:
    1. Import `fonts` from theme (add to existing import)
    2. Add `fontFamily: fonts.regular` to the `input` style

    **Badge.tsx** — Already well-themed. No changes needed (uses colors.accent/error/success).

    **TopicChip.tsx** — Add subtle border on unselected state for visibility:
    1. Add to `unselected` style: `borderWidth: 1, borderColor: colors.border`
    2. Add to `selected` style: `borderWidth: 1, borderColor: colors.accent`

    **Skeleton.tsx** — Change pulse base color from `colors.border` (#1E293B) to `colors.surfaceElevated` for better dark-mode visibility:
    1. Change `backgroundColor: colors.border` to `backgroundColor: colors.surfaceElevated`

    **Toast.tsx** — Already uses theme colors for backgrounds. The text icons (checkmark, X, info symbol) will be replaced by Lucide icons in Plan 13-03. For now, ensure Toast uses borderRadius.md (currently sm), and add glass border:
    1. Update `borderRadius: borderRadius.md` in toast style
    2. Add `borderWidth: 1, borderColor: 'rgba(255, 255, 255, 0.08)'` to toast style for glass feel
  </action>
  <verify>
    Run `npx tsc --noEmit` from ios/ directory. Zero errors related to Button style prop. Confirm `glass` is exported from theme.ts by checking the export statement exists.
  </verify>
  <done>
    theme.ts exports `glass` object with all tokens. Button.tsx accepts `style` prop (resolving 5 pre-existing TS errors). Card has subtle border. Input uses Geist font. TopicChip has border states. Skeleton uses elevated surface color. Toast has rounded corners and glass border.
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert QuestionCard hardcoded light-mode colors to Night Blue tokens</name>
  <files>
    ios/components/quiz/QuestionCard.tsx
  </files>
  <action>
    QuestionCard has 10+ hardcoded hex values that produce bright white/green/red rectangles on a dark background. Replace ALL inline hex colors with theme-derived values.

    **Replace the entire inline style object for TouchableOpacity options:**

    Replace:
    - `#DCFCE7` (correct answer bg) → `'rgba(34, 197, 94, 0.15)'` (success with alpha)
    - `#FEE2E2` (wrong answer bg) → `'rgba(239, 68, 68, 0.15)'` (error with alpha)
    - `#E5E5E5` (selected bg) → `colors.surfaceElevated`
    - `#F5F5F5` (default bg) → `colors.surface`
    - `#16A34A` (correct border) → `colors.success`
    - `#DC2626` (wrong border) → `colors.error`
    - `#000000` (selected border) → `colors.text`
    - `#E0E0E0` (default border) → `colors.border`

    **Replace synthesis badge colors:**
    - `#6366F1` (synthesisBadge bg) → `colors.accent` (use Soft Gold accent for consistency)
    - `#FFFFFF` (synthesisBadgeText color) → `colors.background` (dark text on gold)

    **Replace the checkmark text** ` ✓` — keep it as text for now (Plan 13-03 will replace with Lucide Check icon).

    **Move inline styles to StyleSheet.create** for performance — extract the option styles into a helper function or pre-defined style objects.

    Create a helper function at the top of the component:
    ```typescript
    const getOptionStyle = (isCorrect: boolean, isWrong: boolean, isSelected: boolean, hasResult: boolean) => ({
      backgroundColor: isCorrect && hasResult
        ? 'rgba(34, 197, 94, 0.15)'
        : isWrong
        ? 'rgba(239, 68, 68, 0.15)'
        : isSelected
        ? colors.surfaceElevated
        : colors.surface,
      borderColor: isCorrect && hasResult
        ? colors.success
        : isWrong
        ? colors.error
        : isSelected
        ? colors.text
        : colors.border,
    });
    ```

    Apply this in the render, keeping the static layout properties (padding, borderRadius, minHeight, flexDirection, alignItems, borderWidth) in a StyleSheet entry called `option`.

    Also remove `console.log('[QuestionCard] Render with selectedId:', selectedId);` — this is debug logging that should not remain in production.
  </action>
  <verify>
    Run `npx tsc --noEmit` from ios/ directory. Grep for any remaining hardcoded hex values in QuestionCard.tsx — should find NONE except possibly in comments. Verify `colors.` imports are used throughout.
  </verify>
  <done>
    QuestionCard uses Night Blue theme tokens exclusively. No hardcoded light-mode hex values remain (#DCFCE7, #FEE2E2, #F5F5F5, #E5E5E5, #16A34A, #DC2626, #000000, #E0E0E0, #6366F1, #FFFFFF are all replaced). Debug console.log removed.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes from ios/ with zero new errors (Button style prop errors should be resolved)
2. `grep -r "glass" ios/theme.ts` shows the glass token export
3. `grep -rn "#DCFCE7\|#FEE2E2\|#F5F5F5\|#E5E5E5\|#16A34A\|#DC2626\|#E0E0E0\|#6366F1" ios/components/quiz/QuestionCard.tsx` returns zero matches
4. All 8 UI primitives import from `../../theme` and use `colors.*` tokens
</verification>

<success_criteria>
- theme.ts exports `glass` object with border, fill, shadow, intensity, tint tokens
- Button accepts `style` prop (5 pre-existing TS errors resolved)
- QuestionCard has zero hardcoded light-mode hex values
- All 8 UI primitives use Night Blue theme tokens consistently
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/13-design-system/13-01-SUMMARY.md`
</output>
