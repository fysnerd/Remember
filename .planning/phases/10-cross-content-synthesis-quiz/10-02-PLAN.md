---
phase: 10-cross-content-synthesis-quiz
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - ios/types/content.ts
  - ios/hooks/useQuiz.ts
  - ios/components/quiz/QuestionCard.tsx
  - ios/app/quiz/theme/[id].tsx
autonomous: true

must_haves:
  truths:
    - "Synthesis questions are distinguishable from single-content questions (tagged or labeled so the user can see they test cross-content understanding)"
    - "User sees a Synthese badge on synthesis questions during theme quiz"
    - "Theme quiz screen correctly renders both synthesis and per-content questions without crashes"
  artifacts:
    - path: "ios/types/content.ts"
      provides: "Question type with optional isSynthesis boolean"
      contains: "isSynthesis"
    - path: "ios/hooks/useQuiz.ts"
      provides: "Updated transformCardsToQuiz that passes isSynthesis flag and handles null content"
      contains: "isSynthesis"
    - path: "ios/components/quiz/QuestionCard.tsx"
      provides: "QuestionCard with Synthese badge when isSynthesis is true"
      contains: "synthesisBadge"
    - path: "ios/app/quiz/theme/[id].tsx"
      provides: "Theme quiz screen passing isSynthesis prop to QuestionCard"
      contains: "isSynthesis"
  key_links:
    - from: "ios/hooks/useQuiz.ts"
      to: "/api/reviews/practice/theme"
      via: "api.post fetch returning cards with quiz.isSynthesis"
      pattern: "practice/theme"
    - from: "ios/hooks/useQuiz.ts"
      to: "ios/types/content.ts"
      via: "transformCardsToQuiz maps isSynthesis to Question type"
      pattern: "isSynthesis"
    - from: "ios/app/quiz/theme/[id].tsx"
      to: "ios/components/quiz/QuestionCard.tsx"
      via: "passes current.isSynthesis as prop"
      pattern: "isSynthesis"
---

<objective>
iOS type updates, hook changes, and visual distinction for synthesis questions in the theme quiz flow.

Purpose: Users can visually distinguish synthesis (cross-content) questions from single-content questions during a theme quiz, via a "Synthese" badge on the QuestionCard component.
Output: Updated Question type with isSynthesis, updated useThemeQuiz hook handling synthesis cards, QuestionCard with synthesis badge, theme quiz screen passing the flag.
</objective>

<execution_context>
@C:/Users/vmsan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/vmsan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-cross-content-synthesis-quiz/10-RESEARCH.md
@.planning/phases/10-cross-content-synthesis-quiz/10-01-SUMMARY.md

@ios/types/content.ts
@ios/hooks/useQuiz.ts (full file -- BackendCard interface, transformCardsToQuiz, useThemeQuiz)
@ios/components/quiz/QuestionCard.tsx (full file -- QuestionCardProps, render logic)
@ios/app/quiz/theme/[id].tsx (full file -- ThemeQuizScreen)
@ios/theme.ts (colors, spacing, borderRadius imports)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update types and hooks for synthesis question support</name>
  <files>ios/types/content.ts, ios/hooks/useQuiz.ts</files>
  <action>
1. **Add isSynthesis to Question type** in `ios/types/content.ts`:
   - Add `isSynthesis?: boolean;` to the `Question` interface, after the `explanation` field.

2. **Update BackendCard interface** in `ios/hooks/useQuiz.ts`:
   - The `quiz` object inside `BackendCard` has a `content` field typed as `{ id: string; title: string }`. For synthesis questions, `content` is null (contentId is nullable). Change the type to:
   ```typescript
   content: {
     id: string;
     title: string;
   } | null;
   ```
   - Add `isSynthesis?: boolean;` to the quiz object inside BackendCard.
   - Add `theme?: { id: string; name: string };` to the quiz object inside BackendCard (synthesis cards include theme info).

3. **Update ThemePracticeResponse** in `ios/hooks/useQuiz.ts`:
   - Add `hasSynthesis?: boolean;` and `synthesisCount?: number;` to the `ThemePracticeResponse` interface (after contentCount).

4. **Update transformCardsToQuiz** in `ios/hooks/useQuiz.ts`:
   - In the `cards.map()` callback (line 54), add `isSynthesis: card.quiz.isSynthesis || false` to the returned Question object, after the `explanation` field.
   - The `card.quiz.content` can now be null for synthesis cards. The function currently accesses `card.quiz.content` but only uses it implicitly through the return type. Verify it does not crash -- `content` is included but not directly accessed in the transform. If any access to `card.quiz.content.title` or similar exists, add a null check.

5. **Update useThemeQuiz** in `ios/hooks/useQuiz.ts`:
   - In the return value of the queryFn (line 139), add `hasSynthesis: data.hasSynthesis || false` and `synthesisCount: data.synthesisCount || 0` alongside the existing spread:
   ```typescript
   return {
     ...quiz,
     theme: data.theme,
     contentCount: data.contentCount,
     hasSynthesis: data.hasSynthesis || false,
     synthesisCount: data.synthesisCount || 0,
   };
   ```
  </action>
  <verify>
    - `cd ios && npx tsc --noEmit` passes (or only shows pre-existing errors unrelated to this change)
    - No TypeScript errors about `content` being possibly null in transform function
  </verify>
  <done>
    - Question type has optional isSynthesis boolean
    - BackendCard handles nullable content and includes isSynthesis + theme fields
    - transformCardsToQuiz passes isSynthesis flag through to Question objects
    - useThemeQuiz returns hasSynthesis and synthesisCount from backend response
  </done>
</task>

<task type="auto">
  <name>Task 2: QuestionCard synthesis badge + theme quiz screen integration</name>
  <files>ios/components/quiz/QuestionCard.tsx, ios/app/quiz/theme/[id].tsx</files>
  <action>
1. **Add synthesis badge to QuestionCard** in `ios/components/quiz/QuestionCard.tsx`:

   a. Add `isSynthesis?: boolean;` to the `QuestionCardProps` interface.

   b. Add `isSynthesis = false` to the destructured props.

   c. Before the question `<Text>` element (before line 40's `<Text variant="h3">`) and inside the outer `<View>`, add a conditional synthesis badge:
   ```tsx
   {isSynthesis && (
     <View style={styles.synthesisBadge}>
       <Text variant="caption" weight="medium" style={styles.synthesisBadgeText}>
         Synthese
       </Text>
     </View>
   )}
   ```

   d. Add styles to the `StyleSheet.create` block:
   ```typescript
   synthesisBadge: {
     backgroundColor: '#6366F1',
     paddingHorizontal: 10,
     paddingVertical: 4,
     borderRadius: 12,
     alignSelf: 'flex-start',
     marginBottom: 12,
   },
   synthesisBadgeText: {
     color: '#FFFFFF',
     fontSize: 12,
   },
   ```

   The badge uses indigo (#6366F1) which is the app's primary accent color, consistent with the theme color default. It's a small pill-shaped label above the question text.

2. **Pass isSynthesis in theme quiz screen** in `ios/app/quiz/theme/[id].tsx`:

   In the `QuestionCard` usage (around line 122-129), add the `isSynthesis` prop:
   ```tsx
   <QuestionCard
     question={current.question}
     options={current.options}
     selectedId={selectedAnswer}
     onSelect={(optionId) => {
       setSelectedAnswer(optionId);
     }}
     isSynthesis={current.isSynthesis}
   />
   ```

   This passes the `isSynthesis` field from the current Question object (populated by transformCardsToQuiz from the backend) to the QuestionCard component.

   No other screens need changes -- the content quiz screen (`quiz/[id].tsx`) and topic quiz screen (`quiz/topic/[name].tsx`) never have synthesis questions, so the default `false` value is correct.
  </action>
  <verify>
    - `cd ios && npx tsc --noEmit` passes (or only shows pre-existing errors)
    - `cd ios && npx expo export --platform ios` succeeds (bundle compiles)
    - Visually verify that QuestionCard has a synthesisBadge style defined
  </verify>
  <done>
    - QuestionCard shows indigo "Synthese" pill badge above question text when isSynthesis is true
    - Theme quiz screen passes isSynthesis from current question to QuestionCard
    - Non-synthesis questions render exactly as before (no visual change)
    - Badge is visible only during theme quizzes that include synthesis questions
  </done>
</task>

</tasks>

<verification>
1. iOS compiles: `cd ios && npx tsc --noEmit` passes (or only pre-existing errors)
2. iOS bundles: `cd ios && npx expo export --platform ios` succeeds
3. Question type in content.ts has isSynthesis optional boolean
4. BackendCard in useQuiz.ts has nullable content and isSynthesis field
5. transformCardsToQuiz maps isSynthesis from backend card to Question
6. useThemeQuiz returns hasSynthesis and synthesisCount
7. QuestionCard has synthesisBadge styles and conditional render
8. theme/[id].tsx passes isSynthesis to QuestionCard
</verification>

<success_criteria>
- Synthesis questions display a visible "Synthese" badge (indigo pill) above the question text in theme quizzes
- Regular per-content questions display exactly as before (no badge, no visual change)
- The iOS app correctly handles synthesis cards where content is null (no crashes)
- Type system properly reflects the optional isSynthesis field throughout the data flow
</success_criteria>

<output>
After completion, create `.planning/phases/10-cross-content-synthesis-quiz/10-02-SUMMARY.md`
</output>
