---
phase: 12-foundation-build
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "App runs on physical iOS device with Night Blue dark mode, no white flash"
    - "Geist font renders at all 5 weights on-device"
    - "Lucide icons render on-device in New Architecture + React 19 environment"
    - "BlurView shows real blur effect on physical device"
    - "Production binary is submitted to TestFlight via eas build --auto-submit"
  artifacts: []
  key_links: []
---

<objective>
Build the app for preview, validate all technologies on a physical iOS device, then ship a production build to TestFlight.

Purpose: Phase 12 requires on-device proof that expo-blur, lucide-react-native, and Geist work correctly on the actual stack (New Architecture + React 19 + RN 0.81.5). A preview build validates before committing to production. The production build with --auto-submit ships to TestFlight for the user.
Output: Preview build validated on device, production build submitted to TestFlight.
</objective>

<execution_context>
@C:/Users/vmsan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/vmsan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-foundation-build/12-01-SUMMARY.md
@ios/app.json
@ios/eas.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit changes, push to remote, and trigger preview build</name>
  <files></files>
  <action>
**Step 1: Commit all Phase 12 changes**

Stage and commit all modified files from Plan 12-01:
```bash
cd C:\Users\vmsan\Desktop\FREE\PB\VIBE\Remember
git add ios/package.json ios/package-lock.json ios/app.json ios/theme.ts ios/app/_layout.tsx ios/components/ui/Text.tsx ios/components/ui/Input.tsx ios/app/dev-test.tsx ios/app/(tabs)/_layout.tsx ios/app/(tabs)/profile.tsx
git commit -m "feat(v3): foundation build -- Night Blue dark mode, Geist font, native deps"
```

If there are any other modified files in `ios/` (like `node_modules` artifacts), add them too. Do NOT stage `.planning/` files in this commit -- they are separate.

**Step 2: Push to remote**
```bash
git push origin master
```

**Step 3: Trigger preview build**
```bash
cd C:\Users\vmsan\Desktop\FREE\PB\VIBE\Remember\ios
eas build --profile preview --platform ios
```

This will take 10-20 minutes. Wait for the build to complete. The output will include a build URL and a QR code / link to install on device.

If the build fails:
- Read the build logs from the EAS dashboard URL provided in the output
- Common issues: dependency version mismatch (fix in package.json and retry), native module config error (check app.json plugins), TypeScript compilation error (fix and recommit)
- Fix the issue, commit, push, and retry the build
  </action>
  <verify>
1. `git log --oneline -1` shows the foundation build commit
2. `eas build:list --platform ios --limit 1` shows a preview build in progress or completed
3. Build completes successfully (exit code 0, or EAS dashboard shows "Finished")
  </verify>
  <done>
All Phase 12 code changes are committed and pushed. A preview build has been successfully created via EAS Build.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Validate preview build on physical iOS device</name>
  <files></files>
  <action>
Present the build install link to the user and ask them to validate on their physical iOS device. The preview build contains Night Blue dark mode, Geist font, expo-blur, lucide-react-native, and a validation test screen.

The user must verify these 5 checks:

**Check 1 -- Dark mode + no white flash:**
- App launches with Night Blue (#0A0F1A) background
- Status bar shows white text/icons
- NO white flash between splash screen and app content
- Navigate between tabs -- transitions should NOT show white background

**Check 2 -- Geist font:**
- All text throughout the app should look different from system font (Geist is geometric sans-serif, distinct from SF Pro)
- Navigate to Profile tab > Dev Tools > "Foundation Test"
- On the test screen, verify 5 different font weights are visually distinguishable

**Check 3 -- Lucide icons:**
- On the Foundation Test screen, verify 5 icons render (Home, Search, User, Heart, Star)
- They should appear as gold-colored vector icons, NOT as empty boxes or missing glyphs

**Check 4 -- BlurView:**
- On the Foundation Test screen, verify the blur section shows a frosted glass effect over the colored background

**Check 5 -- Overall app usability:**
- Navigate through Feed, Library, Memos, Profile tabs
- Text should be readable (light text on dark background)
- Cards, buttons, inputs should all have dark styling

If Lucide icons do NOT render (blank/crash), report this -- the fallback is expo-symbols (SF Symbols). This is expected as a possibility and does not block the production build.

Ask user: Type "approved" if all 5 checks pass, or describe any issues you see.
  </action>
  <verify>User confirms "approved" or issues are fixed and re-validated.</verify>
  <done>Preview build validated on physical iOS device. All technologies confirmed working on New Architecture + React 19.</done>
</task>

<task type="auto">
  <name>Task 3: Ship production build to TestFlight</name>
  <files></files>
  <action>
After user approves the preview build, trigger the production build with auto-submit to TestFlight:

```bash
cd C:\Users\vmsan\Desktop\FREE\PB\VIBE\Remember\ios
eas build --profile production --platform ios --auto-submit
```

This will:
1. Create a production binary (10-20 min build time)
2. Auto-increment the build number (from current ~8)
3. Automatically submit to App Store Connect / TestFlight
4. The `submit.production.ios` config in `eas.json` has the Apple credentials already configured

Wait for build completion. The output will confirm submission status.

If auto-submit fails but the build succeeds, manually submit:
```bash
eas submit --profile production --platform ios --latest
```

**If Lucide failed in preview and user opted for expo-symbols fallback:**
Before the production build, install expo-symbols and update the dev-test screen:
```bash
npx expo install expo-symbols
```
Replace Lucide imports with `import { SymbolView } from 'expo-symbols'` and render `<SymbolView name="house.fill" tintColor="#D4A574" size={28} />` etc. Commit and push before building.
  </action>
  <verify>
1. `eas build:list --platform ios --limit 1` shows a production build completed
2. EAS output confirms "Submitted to App Store Connect" or similar success message
3. The build appears in TestFlight (may take 5-15 min after submission for Apple processing)
  </verify>
  <done>
Production binary with Night Blue dark mode, Geist font, and all native dependencies is built and submitted to TestFlight. Phase 12 is complete -- all future v3.0 visual work can be deployed via OTA updates without native rebuilds.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Preview build validated on physical device -- all 5 checks passed
2. Production build successfully submitted to TestFlight
3. App version is 1.1.0 with auto-incremented build number
4. All native dependencies (expo-blur, react-native-svg, lucide-react-native) are in the binary
5. Geist font renders correctly on-device
6. Dark mode active with no white flash
</verification>

<success_criteria>
A production binary is on TestFlight with Night Blue dark mode, Geist font, and all v3.0 native dependencies. On-device validation confirms BlurView, Lucide icons, and Geist font work correctly on New Architecture + React 19. All future Phase 13-16 work is OTA-deployable.
</success_criteria>

<output>
After completion, create `.planning/phases/12-foundation-build/12-02-SUMMARY.md`
</output>
