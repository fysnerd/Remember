---
phase: 05-theme-data-model-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
autonomous: true

must_haves:
  truths:
    - "Theme table exists in database with name, slug, color, emoji, and user ownership"
    - "ContentTheme join table exists with contentId, themeId, assignedAt, assignedBy"
    - "ThemeTag table exists linking themes to tags for deterministic classification"
    - "Prisma client generates without errors and TypeScript compiles with new models"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "Theme, ContentTheme, ThemeTag models with all relations"
      contains: "model Theme"
  key_links:
    - from: "Theme"
      to: "User"
      via: "userId foreign key"
      pattern: "userId.*String"
    - from: "ContentTheme"
      to: "Content"
      via: "contentId foreign key"
      pattern: "contentId.*String"
    - from: "ContentTheme"
      to: "Theme"
      via: "themeId foreign key"
      pattern: "themeId.*String"
    - from: "ThemeTag"
      to: "Tag"
      via: "tagId foreign key"
      pattern: "tagId.*String"
---

<objective>
Add Theme, ContentTheme, and ThemeTag models to the Prisma schema and push to the database.

Purpose: Establishes the data foundation for theme-centric navigation. All subsequent API and UI work depends on these tables existing.
Output: Updated schema.prisma with 3 new models, back-relations on User/Content/Tag, database synced via `prisma db push`.
</objective>

<execution_context>
@C:/Users/vmsan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/vmsan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-theme-data-model-api/05-RESEARCH.md

@backend/prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Theme, ContentTheme, and ThemeTag models to Prisma schema</name>
  <files>backend/prisma/schema.prisma</files>
  <action>
Add three new models to `backend/prisma/schema.prisma` in a new section after the Tags section (`// ============================================================================ // Themes // ============================================================================`).

**Model 1: Theme**
```
model Theme {
  id          String         @id @default(cuid())
  userId      String
  user        User           @relation(fields: [userId], references: [id], onDelete: Cascade)
  name        String
  slug        String
  color       String         @default("#6366F1")
  emoji       String         @default("📚")
  contentThemes ContentTheme[]
  themeTags     ThemeTag[]
  createdAt   DateTime       @default(now())
  updatedAt   DateTime       @updatedAt

  @@unique([userId, slug])
  @@unique([userId, name])
  @@index([userId])
}
```

**Model 2: ContentTheme (explicit M:N join table)**
```
model ContentTheme {
  id          String   @id @default(cuid())
  contentId   String
  content     Content  @relation(fields: [contentId], references: [id], onDelete: Cascade)
  themeId     String
  theme       Theme    @relation(fields: [themeId], references: [id], onDelete: Cascade)
  assignedAt  DateTime @default(now())
  assignedBy  String   @default("system")

  @@unique([contentId, themeId])
  @@index([themeId])
  @@index([contentId])
}
```

**Model 3: ThemeTag**
```
model ThemeTag {
  id       String @id @default(cuid())
  themeId  String
  theme    Theme  @relation(fields: [themeId], references: [id], onDelete: Cascade)
  tagId    String
  tag      Tag    @relation(fields: [tagId], references: [id], onDelete: Cascade)

  @@unique([themeId, tagId])
  @@index([themeId])
  @@index([tagId])
}
```

**Also add back-relations to existing models:**
- On `User` model: add `themes Theme[]` in the Relations section (after `pushTokens`)
- On `Content` model: add `contentThemes ContentTheme[]` in the Relations section (after `transcriptCache`)
- On `Tag` model: add `themeTags ThemeTag[]` (after `contents`)

IMPORTANT: Do NOT change any existing fields or relations. Only ADD the new relation arrays and new models.
  </action>
  <verify>
Run `npx prisma validate` in the backend directory to verify schema is valid. Then run `npx prisma generate` to verify the Prisma client generates without errors. Finally run `npx tsc --noEmit` to verify TypeScript still compiles.
  </verify>
  <done>
schema.prisma contains Theme, ContentTheme, and ThemeTag models. User, Content, and Tag models have back-relation fields. `prisma validate` passes. `prisma generate` succeeds. `tsc --noEmit` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Push schema to database and verify tables exist</name>
  <files>backend/prisma/schema.prisma</files>
  <action>
Deploy the schema changes to the production database on VPS. The project uses `prisma db push` (no migrations directory exists).

Steps:
1. Commit the schema changes to git and push to remote
2. SSH into VPS: `ssh root@116.203.17.203`
3. On VPS: `cd /root/Remember/backend && git pull && npx prisma db push && npx prisma generate && npm run build && pm2 restart remember-api`
4. Verify the tables were created by checking PM2 logs: `pm2 logs remember-api --lines 10`

The `prisma db push` command will create the Theme, ContentTheme, and ThemeTag tables without data loss (additive changes only -- new tables and new nullable relation fields on existing tables).

NOTE: Since Content and Tag models only get new relation arrays (not new columns), the database changes are purely additive (3 new tables). No existing data is affected.
  </action>
  <verify>
After VPS deployment, verify the API starts without errors by checking `pm2 logs remember-api --lines 10` shows "Ankora API started" and no Prisma errors. Also hit the health endpoint: `curl https://api.ankora.study/health` returns `{"status":"ok"}`.
  </verify>
  <done>
Theme, ContentTheme, and ThemeTag tables exist in the production database. Backend compiles and runs without errors on VPS. Health endpoint responds OK.
  </done>
</task>

</tasks>

<verification>
1. `prisma validate` passes locally with the updated schema
2. `prisma generate` succeeds locally
3. `tsc --noEmit` passes locally
4. VPS deployment succeeds (`pm2 logs` shows clean start)
5. `curl https://api.ankora.study/health` returns 200
</verification>

<success_criteria>
- Three new tables (Theme, ContentTheme, ThemeTag) exist in the database
- Prisma client types include Theme, ContentTheme, ThemeTag models
- Existing functionality is unaffected (no breaking changes to Content, Tag, or User models)
- Backend runs cleanly on VPS after deployment
</success_criteria>

<output>
After completion, create `.planning/phases/05-theme-data-model-api/05-01-SUMMARY.md`
</output>
